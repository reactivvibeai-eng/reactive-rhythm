# SESSION STATE — Reactive Rhythm (reload point; read with CLAUDE.md)

## Deploy workflow
- Game lives in the Lovable repo **reactiv435/reactivvibeailive** at **public/games/reactive-rhythm/** →
  serves at **reactivvibe.com/games/reactive-rhythm/**.
- Claude pushes GAME updates DIRECT to `main` via git (PAT in the local remote at `D:\...\cloudcode\reactivvibeailive`).
  Loop: edit `v2/` (dev source) → `node --check` → copy changed file(s) into `reactivvibeailive/public/games/reactive-rhythm/`
  → `git -C <reactivvibeailive> pull; add; commit; push main` → Lovable redeploys.
- `v2/` = dev source. Bump `?v=NN` in index.html on external JS/CSS change. **Currently v47.**
- Lovable also edits the React app (`src/`) — do NOT touch `src/`; hand Lovable briefs.

## 5-STRING GUITAR HERO MODE (active build — Phase 0a DONE, local only, NOT deployed)
GOAL: a 5-fret/5-string mode for a real Guitar-Hero controller, **additive** to the proven 6-lane keyboard game.
- **Lane-profile architecture (game.js):** `LANE_COUNT` is now a `let` driven by `LANE_PROFILES` — `standard`
  (6-string, byte-identical to before) + `gh` (5-string). `applyLaneProfile(name)` swaps count + ART geometry +
  LANE_COLORS + keymap (own store `rr_keymap_gh`) + guitar image (`activeGuitarImg`) + re-allocs per-lane arrays.
  `buildNotes` span is clamped to LANE_COUNT (5-safe). Boot: `?gh=1` or saved `rr_lanemode==='gh'` → gh.
  Console hooks: `__rrLaneMode('gh'|'standard')`, `__rrGetLaneMode()`. Verified headless: gh→5 lanes/ASDJK,
  standard→6/ASDJKL, live toggle both ways, node-valid, zero console errors. Standard users hit NO new code path.
- **Art:** `assets/guitar5.png` (1080×1920, aspect 0.5625). ⚠️ The master `guitar5-orig.png` (2160×3840) has a
  FAKE checkerboard background BAKED IN as opaque pixels (NOT real alpha) — so `guitar5.png` is generated by
  flood-fill keying the border background to transparent (the `BgKey` C# helper; cleared ~45%). If you ever
  regenerate from the master, you MUST re-key it transparent or the game draws an opaque gray block on the moon.
  Measured geometry (FIRST PASS — pending user visual confirmation on their machine): `nutFY 0.205`,
  `nutXF [0.440,0.477,0.503,0.530,0.567]`; `bridgeFY 0.84`, `bridgeXF [0.330,0.408,0.470,0.548,0.628]`.
  Realistic-bass string fan is NARROWER than the old game-optimized art → playfield lands tighter; may add a
  per-profile "zoom into the neck" if it feels cramped. Re-tune via `measure.html` (auto-detect + drag calibrator).
- **Colors:** GH green/red/yellow/orange with the BLUE fret CHROME-SWAPPED (brand rule: no blue/purple).
- **Mode entry (chosen: BOTH auto + toggle):** only `?gh=1` + persistence + console hook so far. Visible toggle
  UI + auto-on-controller-connect = Phase 0b (NOT built yet).
- **Controller (recommended buy):** CRKD Gibson Les Paul, **Multi-Platform** SKU (Pro ~$125 mechanical strum, or
  Black Tribal ~$100). Connect via dongle/USB-C, not Bluetooth. Default gamepad map to ship as a guess, then
  CALIBRATE LIVE: frets G0 R1 Y3 B2 O4 (yellow/blue often swapped!), strum dpad btn 12/13, whammy axis2, tilt
  axis3. User is ordering one off Amazon. GH-API gotcha: a guitar doesn't appear in getGamepads() until first strum.
- **Phased plan (user picked):** 0=5-string board on keyboard ✅ → 1=tap-mode controller + live calibrator →
  2=authentic strum (hold fret + strum bar) + whammy/tilt. `measure.html`, `guitar5-orig.png`, dev hooks = DO NOT DEPLOY.
- **CONTROLLER CONFIG UI (v50, works in BOTH modes — deployable to live beta):** Settings → "Controller
  Buttons" — click a lane, press the controller button to map it (mirrors the keyboard remap). Remappable
  `padMap` (button→lane) persisted per profile (`rr_padmap` / `rr_padmap_gh`), default = identity (button
  0..N-1 → lane 0..N-1, so existing controller behaviour is unchanged until remapped). `pollGamepad` rewritten
  to route through padMap AND now drives laneDown/onLaneRelease, so a controller can finally HOLD sustain notes.
  Built because a beta tester got a PS5 DualSense that detected but couldn't be configured. This is also the
  GH-guitar live calibrator. Verified: renders 6 chips standard / 5 gh, no errors. NOT deployed yet.
## ⏭️ NEXT AGENT — START HERE (handoff @ v65)
You're continuing a VISUAL OVERHAUL of the 5-string Guitar-Hero mode. Read this whole VISUAL OVERHAUL block,
then CLAUDE.md (constraints) + the WS sections below. Quick orientation:
- **Branch `visual-overhaul`** (on it now), off `main` = known-good v59 (commit 96a2949). `main` is the safe
  rollback; DO NOT deploy / push to Lovable until the user explicitly says — the LIVE site works well + has happy
  beta testers. Test loop: `python serve.py` (8787) serves the working dir → user reloads `localhost:8787/index.html?gh=1`.
- **The 5-string "gh" mode** is a LANE PROFILE in game.js (`LANE_PROFILES.gh`); standard 6-string is byte-identical
  and must STAY that way. gh is gated by `?gh=1` / Settings→Lane Mode toggle / saved `rr_lanemode`.
- **Verify visuals via OFFLINE PowerShell renders** (System.Drawing → PNG → Read) replicating the game math —
  live-canvas screenshots TIME OUT. DOM (HUD/jukebox) is checkable via preview_inspect/snapshot. `node --check
  game.js` after every JS edit. Bump `?v=NN` in index.html on every JS/CSS edit. Commit small per increment.
- **THE PENDING DECISION (ask user / they were choosing):** next build is either (1) **WS4 audio-reactive
  WAVEFORM on the Browse page** (additive, lower-risk), or (2) **more NOTE-TYPE VARIETY in gameplay** (HOPOs /
  open notes / double-notes / trills — GH-style) which is GAMEPLAY SURGERY on buildNotes+render+hit-detection,
  MUST be flag-gated + USER PLAYTESTS each (gameplay currently "feels good" — don't destabilize blind).
- **Image gen IS connected:** `mcp__357d4c14…__generate_image` (use `get_cost:true` to preflight + show user
  before spending). Approach = procedural-first (CSS+Canvas); generate textures only where they truly help.
- **Research (4 full reports):** `…/tasks/wpeulou1g.output` — perspective[applied], HUD/UI, song-select, combo-FX.
  Plus earlier overnight research `…/tasks/w68ot6ks8.output` (GH mechanics — HOPO/scoring/calibration).
- **gh geometry (game.js LANE_PROFILES.gh):** `fit:'cover'` (fills playfield, body to edges, headstock cropped),
  `bottomAnchor:0.93`, `persp:4` (1/z highway depth; `?persp=N` override), `nutFY:0.16 bridgeFY:0.81`,
  `nutXF/bridgeXF` line-fit measured + far-end vanishing convergence in fretGeom (cv 0.34, gh-only). guitar5.png
  (background flood-fill keyed; master guitar5-orig.png has a BAKED checkerboard — re-key if regenerated).

## VISUAL OVERHAUL (branch `visual-overhaul`, off `main` checkpoint 96a2949 = known-good v59)
Goal: make it look/feel like a real GAME ENGINE, not an "AI website." User is iterating with us; DO NOT push to
the live site / Lovable until user says — what's deployed works well. All overhaul work is on the branch.
- **Approach chosen:** procedural-first (CSS+Canvas — that's what reads as a game engine; raster AI assets read as
  "AI"). Image-gen IS connected to the session: MCP `mcp__357d4c14...__generate_image` (use `get_cost:true` to
  preflight credit cost + show the user BEFORE spending). So no ChatGPT key needed.
- **Research (4 reports, FULL output saved):** `…/tasks/wpeulou1g.output` — (1) perspective [APPLIED], (2) HUD/UI,
  (3) song-select, (4) combo-FX. Read #2/#3/#4 when building WS3/WS4/WS2-remaining.
- **WS1 — THE HIGHWAY = DONE (v60-61):** 1/z PERSPECTIVE note map (far notes bunch+crawl, near notes spread+
  accelerate = depth/vertigo) — gh profile `persp:4`, `?persp=N` URL override to A/B (3=subtle, 6=intense).
  Scrolling FRET LINES (highway rushes at you). Top FADE-INTO-MOON (cover-fit fades the screen-top). Glowing SPAWN
  band. All gh-only (standard 6-string byte-identical); HIT TIMING UNCHANGED (uses note time, not screen pos).
  User confirmed "playing feels good." Implemented in render() noteX/noteY + P()/depthScale + fret loop; fade at
  the guitar-draw block.
- **WS2 — BOOST/COMBO FX = STARTED (v59 scan, v62 wash):** combo SCAN sweep (amber→crimson, on OD activation +
  every combo%25, tier-brightened, `scanT`/`scanTier`), OD-active COLOR-CYCLING beat-pulsed wash (kills the
  "static yellow line"). REMAINING: per-tier combo VARIETY (distinct effect/color at 25/50/100), particles,
  chromatic split, forked lightning recolor, and TEXTURE the Overdrive meter fill (the literal "yellow line").
- **WS3 — HUD/METERS = STARTED (v63):** meters (`.song-progress`: Overdrive + stability) rebuilt as machined
  INSET tracks (beveled, inner-shadow) with glossy energy fills + a moving "powered" SHEEN sweep. REMAINING:
  deeper HUD framing (panels/score/judgment as framed game-asset components), iconography, scanline/vignette
  pass — see research report #2 (HUD/UI) in the workflow output.
  v63-65 also added: combo number BLACK OUTLINE (readable over guitar), and a faint screen-space SCANLINE on
  `.game-screen` (CRT game feel).
- **WS4 Browse/Jukebox = barely started (v64):** living shimmer + aura on the FOCUSED coverflow cover
  (`.jb-cover.is-center .cover-card::after`, reactive feel). REMAINING + user-requested: **audio-reactive
  WAVEFORM on the browse page** (driven by the song-preview audio — main ask, "keep it reactive"), momentum-
  scroll feel, the genres/artists tiles + all-songs LIST polish (user says those read "weak/AI"). Coverflow has
  good bones (3D, motion-blur, center bloom) — enhance, don't rebuild. Research report #3 (song-select).
- **WS2 also got (v65):** combo-reactive flowing ENERGY TEXTURE on the guitar (2 warm bands, brighter/faster
  with combo tier — `combo`/`cTier` in render). WS2 still wants: per-tier DISTINCT combo effects + the GH-style
  NOTE-TYPE VARIETY (the user's gameplay ask — see PENDING DECISION up top; flag-gated + playtested).
- **WS5 Design System = NOT STARTED.**
- **Tasks tracked** (TaskList #1-5). **?v at 65.** Branch commits: 96a2949(v59 base) → c60572a(persp) →
  79c1e4a(Highway done) → 1f127e5(boost wash) → 2dee27b(handoff) → 77c6bc9(v63 meters) → 1b7c58c(v64 shimmer+
  scanline) → 1b...(v65 convergence+combo-outline+combo-texture). All clean, node-checked.

## (pre-overhaul history)
- **?v at 55.** Version history: 47→48 profile system; 49 = gh transparency key-out + tap-zone trim;
  50 = controller config UI; 51 = guitar adaptive zoom; 52 = level campaign; 53 = in-game lane-mode toggle
  (Settings); 54-55 = MOBILE/PHONE fixes.

## MOBILE / PHONE FIXES (v54-55 — beta tester "doesn't scroll or look good on phone")
Root cause was global: `.screen` (index.html:81) centers content with NO scroll, and `#app` was `height:100vh`
(vs html/body 100dvh) → any content taller than the phone viewport got centered & CLIPPED with no way to scroll.
Hit the jukebox (Play button cut off the right edge; coverflow `flex:1` made a big gap; bottom clipped), AND the
How-to-Play (956px) + Calibration (858px) overlays. Fixes (all verified at 375×620 via bounding-box measurement,
screenshots time out on the animated bg): (1) `#app` → `height:100dvh`. (2) jukebox.css mobile block ≤640: action
row WRAPS (Play full-width on top, BROWSE/ALL SONGS share row below). (3) jukebox.css mobile block ≤900:
`#view-jukebox` scrolls; `.jukebox` coverflow gets fixed `height:clamp(230px,50vh,330px)` (kills the gap +
flex:1 greed); `touch-action:pan-x` so vertical drag = page scroll, horizontal = change song; How-to + Profile
overlays `align-items:flex-start; overflow-y:auto` with card `margin:auto`; `#calib-screen .calib-wrap`
`max-height:92dvh; overflow-y:auto`. Verified: jukebox scrolls (scrollH>clientH), Play fits & reachable, overlays
scroll. REMAINING mobile polish (audit flagged, not yet done): search inputs should be font-size≥16px to stop
iOS zoom-on-focus; double-check the All-Songs list + Browse tiles + in-game HUD on a real device.
- **GH GUITAR (v56 — re-fixed after user feedback):** the v51 adaptive zoom made the guitar "way too big" on
  the user's real screen (zoom overshot, guitar wider than canvas) AND the v43-era measurements were ~15% too
  narrow at the bridge → misaligned. FIX: zoom REMOVED (gh profile `fanTarget:0, zoomMax:1` = plain contain-fit,
  whole guitar visible, never bigger than the canvas at any resolution). RE-MEASURED rigorously: auto-detected
  the 5 string peaks at two clean rows (y=0.25 and y=0.78), fit each string to a line, extrapolated to spawn/catch
  rows. New gh geometry: `nutFY:0.16, bridgeFY:0.85, nutXF:[0.4622,0.4832,0.5032,0.5265,0.5488],
  bridgeXF:[0.3187,0.4061,0.4934,0.5784,0.6645]`. Verified via offline overlay+render (`_gh_check.png`): catchers
  on the 5 saddles, notes ride the strings, guitar contained. Alignment is now resolution-independent (lanes are
  fractions of the guitar, contain-fit never overflows). PENDING: user confirms on their screen.
  Earlier end-to-end test still valid: chart builds & plays on lanesUsed [0,1,2,3,4], zero errors.
  Mode entry: in-game toggle (Settings → Lane Mode) + ?gh=1. Auto-on-connect still TODO.

## LEVEL SYSTEM / CAMPAIGN (v52 — built & verified overnight; ?dev-gated until release)
Real campaign replacing the random-sample scaffold (index.html inline `<script>` near the LEVELS comment):
- **3 tiers × 6 = 18 levels.** WARM-UP/easy, PULSE/medium, FRACTURE/hard.
- **Stable level→song binding:** persisted in `rr_levelmap` (chosen once by even stride from ready catalog;
  a level is ALWAYS the same song even as catalog grows). 872 catalog songs available.
- **Stars (per difficulty):** read from `rr_scores[trackId|difficulty]` grade — S/A=3, B=2, C/D=1, none=0.
- **Sequential unlock:** level N unlocks when N-1 has ≥1 star. Tier headers show ★ earned/possible. Progress
  bar + "★ total/54". A "NEXT" badge marks the next playable level.
- **Gating:** `CAMPAIGN_PUBLIC=false` (testers see locked + free-play message); `?dev=1` runs the REAL
  progression for us; a dev "⚿ unlock all / ↩ progression" toggle lets us jump to any level for testing.
- Click a level → setDifficulty(tier) → RhythmCatalog.launchTrack. Verified: renders 18 cards, 17 locked at
  start, launch works (menu→loading), no errors. **To launch publicly: flip CAMPAIGN_PUBLIC=true.**
- NOT YET: post-level "return to campaign / play next" loop (results flow), custom per-level art/video (assets
  come from user). Stars refresh whenever the Levels screen is reopened (rebuilds from rr_scores).

## RESEARCH-BACKED ROADMAP (overnight research; playtest-dependent — do WITH user)
Game is already strong: it ALREADY has audio-offset calibration + per-event input-lag compensation (response
time = solid, this is why it felt good), per-hit flair by accuracy, and combo-break feedback. Genuine remaining
upgrades (each needs the user to PLAYTEST/SEE — do not ship blind): (1) Overdrive DOUBLES the multiplier +
earn from marked phrases [changes the score ceiling — make it an opt-in mode]; (2) raise combo multiplier cap
to 4x with a chunky tier-up meter; (3) HOPO "flow" notes for fast runs; (4) NPS-banded density per difficulty
+ protect rest gaps; (5) BPM detection + onset quantization so auto-charts sit on-beat. Full reports in the
overnight research workflow output.

## Built (in the deployed game; normal play byte-identical)
- **Beta gate** (inline) — `/beta/status` + code form `/beta/redeem`. FAILS OPEN until backend exists.
- **Boss Stage v1** — `?boss=1`: forced fail-on, harder drain (1.8x; 2.4x enraged @60%), BOSS DEFEATED/boss-win. `RhythmGame.playBoss`.
- **Account leaderboards** (catalog.js `recordLocal`) — in-browser runs POST `/score` + render leaderboard UI. Dormant until `/score` exists; logged-out → sign-in nudge.
- **Level Picker LOCKED** — testers see "SOON" cards + banner; `?dev=1` unlocks (persists `rr_dev`).
- **FPS meter HIDDEN** — `?fps=1` shows it.
- **Jukebox** — opens on RECENT (`'new'`) by default; prominent **"All Songs · 872"** button → full searchable/sortable catalog.
- **Desktop icon** → crimson moon + guitar (`v2/assets/reactive-rhythm.ico`).

## Dev flags (us): `?dev=1` (unlock levels) · `?boss=1` · `?fps=1` · `?mock` · `?novideo`

## Lovable to-build (prompts in v2/):
- **BETA_ADMIN_LOVABLE_PROMPT.md** — beta backend + `/admin/beta` panel. Activates the gate.
- **/score endpoint** (token-less, account-linked → `game_plays`; spec in chat). Activates leaderboards.
- **GAMING_HUB_BRIEF.md** — `/gaming` hub + nav tab ✅ shipped; Lovable now adding hero/bg videos (visual upgrade in progress).

## Pending — Claude builds (NO assets needed)
- **Core-feel juice upgrades** (researched Guitar Hero): multiplier tier-ups become a moment, combo-streak escalation (50/100/200), Overdrive = full "everything lights up", per-judgment hit flair, live HUD reactivity. NOT built yet.

## GH controller support
- Game already polls gamepads (button index → lane, `pollGamepad`). Classic GH = **5 frets** → needs a **5-lane GH MODE** + a **5-string** guitar asset (lanes ride on strings; current art is 6-string/6-lane). USER is generating the 5-string image (prompt given). On receiving it: re-measure string x-positions, build the 5-lane mode + fret/strum mapping. (GH *Live* = 6 buttons → current 6-string already fits.)

## Collaborative (USER feeds assets, Claude builds core)
- Real **levels + boss stages** + the **5-string GH guitar** + level **video backgrounds/art** = user provides; Claude builds the framework/mechanics.

## Open
- **/games/ 404** — host SPA routing must serve static `/games/*` as files (user + Lovable working it; files ARE on main).
- Then: juice upgrades · beta backend + /score (Lovable) · co-design levels (assets) · GH mode (5-string asset).
