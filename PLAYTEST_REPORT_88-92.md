# Playtest-readiness report — build88-92 (?v=329, workflow wao5t9jnv)

Confirmed blockers: 0 | critiques: 30

## Persona verdicts
- minor-issues :: Brand-new keyboard player, first 5 minutes, never played a rhythm game, no contr
- minor-issues :: A player who heard the game has "AI Flixs" music videos ? walking Browse ? AI FL
- minor-issues :: A returning player with ~3 hours in, browsing the store to spend their first ear
- minor-issues :: A returning player chasing a high score ? replays a known song, builds combo, fi
- minor-issues :: Senior visual/brand art director reviewing Reactive Rhythm's evolved surfaces (s
- ready :: Release QA lead ? final playtest-readiness sweep of build92 (?v=329), hunting on

## SYNTHESIZED REPORT
# Reactive Rhythm ? build88-92 Playtest Synthesis

## A) READINESS VERDICT
**PLAYTEST-READY.** Zero confirmed blockers; 6/6 personas land at "ready" or "minor-issues" (QA lead: ready). The build88-92 surfaces (first-run on-ramp, AI FLIXS discovery, store showroom, OD burn-down) all hold up. Ship the playtest.

## B) CONFIRMED BLOCKERS
None. (Empty = good.)

## C) TOP EXPERIENTIAL CRITIQUES (next-up, ordered by value)

**1. Keyboard newcomer has zero in-canvas key?lane guidance** ? keyboard-safe? NO ? **M**
- No key letter on/under any catcher (`drawCatcher`, game.js:4942); only cue is the flat, non-aligned footer hint. The "TAP THE LANES" countdown coachmark is gated to `body.has-touch` (game.js:2310), so desktop gets nothing. First run = fumbling A/S/D/J/K.
- *Fix:* paint `keyForLane(i)` (game.js:47) dim-under-catcher / lit-on-press, using `nearX[i]`; run a one-time keyboard countdown coachmark behind a new `rr_keycoach_seen` flag.

**2. AI FLIXS "Watch" overlay has no Escape / focus management** ? keyboard-safe? NO ? **S**
- `#flix-watch` (`openWatch`, catalog.js:1120) closes only on ?/scrim. No Escape handler, no focus move, no inert ? the one modal in the app that breaks keyboard-close parity (store-hero got the full treatment in b89/90). Owner plays keyboard; flagged independently by 3 personas.
- *Fix:* capture-phase Escape ? `closeWatch()` while `.open` (mirror store-hero index.html:8123); focus `.fw-close` on open; set page inert; tear down on close.

**3. AI FLIXS discovery has labeling/gating seams** ? keyboard-safe? YES ? **M**
- Films found via **header search** render as plain song rows with no film/Soon cue, and a non-HLS `stream_url` makes them read "playable" (`songCard`, jukebox.js:445; `trackReady`, catalog.js:832). Discovery is gated on a **music** chart-readiness check (`catalogVideos = all.filter(trackReady).filter(isVideo)`, catalog.js:738) so HLS-only films vanish from the grid AND count, though Watch would play them. Poster-grid view still says "songs/tracks/Search songs" (index.html:4527/4531, jukebox.js:540), and the film sheet shows dead Difficulty + Environment pickers (catalog.js:1064).
- *Fix:* `isVideo` branch in `songCard` (Film/Soon tag, suppress chevron/grade); build `catalogVideos` from `all.filter(isVideo)` (no `trackReady` gate); relabel poster view off `songsPoster`; hide `#env-section`+Difficulty in the video branch.

**4. The highest-payoff dopamine beats are under-juiced** ? keyboard-safe? YES ? **M**
- **Beating your own BEST** mid-song is just a corner-chip relabel to "? BEAT BEST" (game.js:3872) ? quieter than a routine 25-combo. The **results "beat it by N" chase CTA** is 12px grey footnote text under the histogram (catalog.js:1273). Both are the literal hooks the returning-player loop runs on.
- *Fix:* at line 3872 fire `_scorePop(true)` + `flashJudgment('NEW RECORD','#e0a93f')` + one-shot shake/bgPulse/vibrate + gold edge-flash (reuse odBurst, lower intensity; existing `.beaten` guard fires it once). Promote the chase line into a framed "NEXT TARGET" card near the score. Cosmetic only ? score/leaderboard untouched.

**5. On-ramp teaches mechanics Easy never shows** ? keyboard-safe? YES ? **S**
- Auto-popped How-To teaches BOMB/CHORD/STAR + 5-lane spread in one wall, but a true first-timer is auto-started on EASY (3 lanes, no bombs, game.js:2624/1397/1142). It also pushes "CALIBRATE MY TIMING" before they've hit a single note.
- *Fix:* tier the How-To to Easy's actual content (or caption "appears on Medium/Hard"); de-emphasize calibrate on first visit, surface it post-first-run.

**6. OD charge ramp + hold-grace boundary are informationally flat** ? keyboard-safe? YES ? **S**
- HUD OD readout creeps `CHARGING?NN%` with no near-full anticipation beat and no signal that stars/streaks feed it (game.js:3898/3146). The hold early-release `0.75` grace cliff (game.js:2728) is invisible, yet a drop breaks combo as hard as a flat miss.
- *Fix:* add an "ALMOST ? NN%" state ?85% + a fill-glow ramp; render a gold "safe-to-release" notch on the hold beam at 0.75.

**Dropped as nits:** PERFECT-word suppression on first hits, 4? mult-cap communication, results 5-button hierarchy, the `.sh-info` :nth-child(8) stagger gap, COMING-SOON dead-end, FLIXS gold-wordmark + type-pairing cohesion, motion-token swap debt, mock-catalog AI-FLIXS invisibility. Worth a cleanup pass, not playtest-blocking.

## D) DELIGHTS (keep / lean in)
- **First-run on-ramp is genuinely thoughtful** ? auto-EASY (3 lanes, no bombs, 1.30 approach, 0.20 window) with an honest toast; unmissable-but-non-trapping How-To that re-arms until acknowledged; welcoming "GET READY ? 3 ? 2 ? 1 ? GO!" pacing.
- **Overdrive teaching is airtight for keyboard** ? how-to + "OVERDRIVE ? SPACE" label + HUD `CHARGING?READY?ACTIVE` walk + one-shot READY flash. A new player knows exactly when/how to fire.
- **build92 OD burn-down is the standout visual win** ? final-1.6s vignette quickens (140?~70ms) and lerps gold?crimson as `odTimer?0` (game.js:4659). Wordless "spend it NOW" cue, pins R=255 (never crosses to purple/blue), `screen`-composited, reads `odTimer` only (scoring untouched), and keeps the color shift under reduce-motion. Textbook juice-within-constraints.
- **Store showroom is the most premium evolved surface** ? spotlit floating guitar with tracked reflection, 3D skins sway / flat cover-art floats-without-spin ("spin reads cheap"), correct gold discipline, full reduce-motion gating, never focusable during play.
- **Real money handled with real care** ? two-click armed Buy with 4s disarm + per-purchase idempotency key; live owned/equipped/coming-soon sync; auto-equip-on-buy payoff; correct modal focus trap + capture-phase Escape priority.
- **Phase-6 "videos aren't playable yet" boundary defended at 4 independent points** (sheet handler, `launchTrack`, deep-link, readiness) ? a shared video link degrades to Watch, never crashes. The `_preview` tag fix (catalog.js:1061) cleanly stops Watch from churning the menu backdrop.
- **Backend-down resilience** ? empty/500 both toast honestly and fall back to the 1000-song mock + `playDemo`; the player always gets a playable run, and the video split never throws on a missing bucket.
- **Brand law actively defended at the token level** ? warm R?G?B darks, de-blued chrome, crimson hairline so dividers can't blend to purple, judgment colors unified 1:1 across HUD and results; the whiff dash was even corrected cool?warm in b89.
- **Keyboard feel is tight and footgun-free** ? key-repeat ignored, Space yields to a remap, the old Space-restart killer is gone, blur/visibility both release-all + auto-pause; empty-press whiff doesn't break combo; responsive score count-up.

**Cross-cutting takeaway:** every "minor-issues" verdict clusters on **two themes** ? (1) the AI FLIXS surface needs a keyboard-Escape + film-vs-song labeling pass, and (2) the returning-player loop's biggest reward moments (beat-best, chase CTA) need the juice the rest of the game already nails. Neither blocks the playtest; both are the clear highest-leverage next sprint.


## ALL CRITIQUES


### [major] No per-lane key labels on the highway ? keyboard player can't map A/S/D/J/K to the colored lanes  (kbSafe=False)
detail: The single biggest learnability gap for a keyboard newcomer. drawCatcher() (game.js:4942) renders the 5 catchers as colored rings ONLY ? no key letter is ever painted on or under a catcher. The only key->lane reference during play is .footer-hint (index.html:5388), a small dim flat mono strip at bottom-center reading 'A S D J K ? ESC PAUSE ? SPACE OVERDRIVE'. It is a flat row, NOT positionally aligned under each lane, so nothing visually links 'A' to the leftmost green lane vs 'K' to the orange one. A first-timer who read the how-to knows the five keys exist but, mid-song with notes falling, has no in-canvas reminder of WHICH key is which lane. They will fumble the left/right mapping on their first run. (The DOM tap-zones DO show key glyphs via ::after, but only on body.has-touch ? index.html:1890 ? so desktop keyboard gets nothing.)
fix: Draw the lane's key letter (keyForLane(i), already available at game.js:47) inside or just under each catcher ring in drawCatcher ? dim by default, lit on press. Even a one-time fade-out label on the first ~5 seconds of a player's first-ever run would close the gap. The data and per-lane x-positions (nearX[i]) are already in hand.

### [major] Countdown 'TAP THE LANES' coachmark is touch-only ? keyboard newcomers get no equivalent  (kbSafe=False)
detail: runCountdown() (game.js:2299-2315) shows a one-time first-run coachmark that pulses the lane outlines with a caption so a player learns where to press ? but it is gated behind body.has-touch (line 2310: `document.body.classList.contains('has-touch')`). A brand-new KEYBOARD player reaching their first 'GET READY ? 3 ? 2 ? 1 ? GO!' gets the countdown with zero key-to-lane teaching moment. The one screen designed to teach 'where do I press' explicitly skips desktop. Combined with the missing catcher labels, the keyboard newcomer's entire first run has no positional key guidance.
fix: Run the same one-time countdown coachmark for keyboard players, captioned with the actual keys per lane (e.g. pulse each lane outline with its A/S/D/J/K glyph). Reuse the existing rr_tapcoach_seen gate idea with a separate rr_keycoach_seen flag.

### [minor] How-To teaches BOMB (and 7 mechanics at once) that the Easy on-ramp never shows  (kbSafe=True)
detail: The auto-popped How-To overlay (index.html:4766) prominently teaches BOMB ('Do not hit ? let the X pass by') plus CHORD, HOLD, STAR and Overdrive ? 7 concepts in one wall. But a true first-run player is auto-started on EASY (game.js:2624), and Easy has bombGap=Infinity (game.js:1397 ? bombs are Hard-only), LANE_SPAN:3 (only A/S/D used, game.js:1142), maxJump:1, and fewer chords. So the newcomer is taught a hazard and a 5-lane spread they will not encounter, then plays a much simpler chart. Mild 'where are the X's / where's the 4th-5th lane?' confusion, and the 7-tile grid is a lot to absorb pre-first-note.
fix: Either tier the How-To to what Easy actually contains (TAP + COMBO + the 3 left lanes first, reveal HOLD/CHORD/BOMB/STAR as difficulty rises), or add a 'these appear on Medium/Hard' caption to the BOMB/CHORD/STAR tiles so the on-ramp player isn't hunting for absent mechanics.

### [minor] How-To pushes 'CALIBRATE MY TIMING' before the player has hit a single note  (kbSafe=True)
detail: The first-run How-To (index.html:4783-4784) tells a brand-new player 'New here? Calibrate your timing first' and offers a CALIBRATE MY TIMING button that opens the settings/calib panel (openCalib). Asking someone who has never seen a note fall to set a millisecond timing offset is premature ? they have no feel yet for whether they're early/late, so the calibration is meaningless and the detour delays their first actual play. It is optional (the primary CTA is 'GOT IT ? LET'S PLAY'), so not a flow-breaker, but it nudges newcomers into a confusing tangent.
fix: De-emphasize calibration on the very first visit (lead with PLAY); surface the calibrate nudge AFTER the first run, e.g. on the results screen or a second-session How-To, when the player has felt the timing and the offset will mean something.

### [minor] Results screen offers 5 action buttons including a 'NEXT LEVEL' that's meaningless in Quick Play  (kbSafe=True)
detail: The results action row (index.html:5449-5455) has PLAY AGAIN, NEXT LEVEL, RETURN TO MENU, CAREER, SHARE SCORE ? five choices for a first-timer to parse. NEXT LEVEL (#results-next) is correctly hidden by default (index.html:1342 `display:none`, revealed only via .show in a campaign sequence), so a Quick-Play run shows four ? still a busy row where the primary next action (PLAY AGAIN / ? ENCORE on a strong run) competes with CAREER and SHARE for a player who just wants to go again. Keyboard flow is well-covered (Enter=replay, Esc=menu, shown at line 5456), which mitigates it.
fix: Visually promote PLAY AGAIN as the clear primary and demote CAREER/SHARE to a quieter secondary tier on a first-ever result, so the newcomer's eye lands on 'go again' without scanning five equal-weight buttons.

### [nit] First PERFECT gives no on-screen word ? slightly muted first-hit reward  (kbSafe=True)
detail: By deliberate GH-feel design (game.js:3158-3162), PERFECT (the common case) does NOT flash a judgment word ? only GREAT/GOOD/MISS do ? to avoid spam at high note rates. This is correct for an experienced player at 5 notes/sec, but on EASY a newcomer's first few clean hits produce catcher-flash + combo-tick but no celebratory 'PERFECT!' pop, which is the single most reinforcing first-hit moment in the genre. The feedback is present (flash, combo, hit chug SFX), just less explicit than a newcomer expects.
fix: Consider showing the PERFECT word for the player's first few hits of a run (or on Easy specifically), then suppress it once a combo is established ? best-of-both: a satisfying first-hit confirmation without late-game spam.

### [major] Watch overlay has no keyboard dismiss (Esc) ? owner plays keyboard  (kbSafe=False)
detail: openWatch() (catalog.js:1120-1144) builds the #flix-watch overlay and wires close ONLY to .fw-scrim click and .fw-close (?) click. There is no Escape handler anywhere ? confirmed: grep for 'Escape'/'flix-watch' in index.html, game.js, and catalog.js finds zero keydown bindings for this overlay, and there is no global overlay-closer that would catch it. The overlay also never moves focus into itself and never sets the page inert, so the underlying jukebox stays Tab-reachable behind the scrim. The owner's primary input is the keyboard; once 'Watch' opens a film, the only way out is a mouse click on ? or the scrim. Every other modal in the app (sheet, store hero) handles Escape ? this one is the regression-prone odd one out. This is the single roughest edge in the journey for the keyboard owner.
fix: Add a document/window keydown handler (capture phase) while #flix-watch.open that calls closeWatch() on Escape, mirroring the store-hero/sheet Escape pattern; on open, move focus to .fw-close and ideally set the rest of the page inert so Tab can't reach behind the scrim. Tear the handler down in closeWatch().

### [major] A film found via HEADER SEARCH renders as a plain song ROW with no 'film/Soon' cue  (kbSafe=True)
detail: The persona's explicit step ('search a video by name') goes through the header search, which calls openSongs(allMedia(), 'Search ? "q"', 'jukebox', q, null, false, 'all') (jukebox.js:615) ? note poster=false. So results render via songCard() as a normal music ROW, not videoCard(). songCard() (jukebox.js:445-491) has NO isVideo branch: the film shows the same square art, the same '? genre ? BPM ? duration' subline, and a right-side chevron/grade ? nothing signals it is a film. Worse, the right-side status comes from trackReady(t); for a video trackReady() is !!trackAudioUrl(t) (catalog.js:832-836), and trackAudioUrl includes a non-HLS stream_url, so a film with a plain mp4 stream reads as 'ready' and shows a play-style chevron, implying it's a playable song. The film/Watch nature only becomes clear after you tap and the sheet opens. So the in-grid experience (poster + 'Soon' badge, coherent) and the in-search experience (anonymous song row, possibly looking 'playable') disagree.
fix: In songCard(), add an isVideo(t) branch: render a small 'Film'/'Soon' tag (reuse .vc-badge styling) and suppress the BPM/grade/chevron for videos, OR have the header-search path render mixed results with videoCard for video rows. Minimum: a visible film marker so a searched film is recognizable before the tap.

### [major] Film discoverability is gated on a MUSIC chart-readiness check, not on watchability  (kbSafe=True)
detail: catalogVideos = all.filter(trackReady).filter(isVideo) (catalog.js:738-740). trackReady() is a music concept (has a decodable audio_url or a server chart). A film whose only source is an HLS .m3u8 (or any source trackAudioUrl rejects) has trackReady()===false and is dropped from catalogVideos entirely ? so it never appears in the AI FLIXS grid AND is excluded from videoCount(), even though videoWatchUrl() (catalog.js:784) would happily Watch that same HLS via the new-tab fallback. The gate meant for 'don't show dead songs' is silently hiding watchable films from a discovery surface where playability is explicitly deferred to Phase 6.
fix: Build catalogVideos from all.filter(isVideo) (no trackReady gate) so every film is discoverable; keep the Watch button's own videoWatchUrl()/disabled logic to decide whether a given film can preview. Discovery should not inherit the music playability gate.

### [minor] AI FLIXS hero vanishes with no explanation when the catalog has no videos (backend-down / mock)  (kbSafe=True)
detail: The hero card only renders when nVid>0 (jukebox.js:286). buildMockCatalog (catalog.js:671-705) emits ZERO videos ? no row carries media_type/is_video, genres come from GENRES (not 'music video'/'ai film'), and titles from TW_A/TW_B won't match the MV/visualizer/lyric heuristic. So under ?mock=1 OR any live-catalog failure (the CHANGELOG notes the live game-catalog has returned HTTP 500 before, degrading to mock), Browse shows NO AI FLIXS entry at all. A player who came specifically for AI Flixs finds nothing and no 'temporarily unavailable' signal ? the journey silently dead-ends in the fallback state. Healthy live data (build88 verified 112 videos) shows it fine, so this is a degradation gap, not a always-on break.
fix: Either seed a few video rows into buildMockCatalog so the section survives the offline/mock fallback, or render the hero in a muted 'AI FLIXS ? films are loading / temporarily unavailable' state when live load failed, so the entry point doesn't silently disappear.

### [minor] Film sheet still shows Difficulty + Environment pickers that do nothing for a Watch  (kbSafe=True)
detail: openSheet()'s video branch sets the Watch handler and returns early at catalog.js:1064, but never hides the always-present 'Difficulty' section (index.html:4564) or the 'Environment' picker (#env-section, index.html:4582). No code anywhere hides these for a video (grep confirms no env-section/Difficulty toggling in catalog.js). So a film's sheet presents Easy/Medium/Hard and an Environment chooser above the '? Watch' button ? controls that are meaningless for a non-playable preview and muddy the 'this is a film, not a chart' message.
fix: In the video branch of openSheet(), hide #env-section and the Difficulty section (e.g. set display:none) before the early return, restoring them on the next music sheet. Leaves only art + meta + Watch + the clear 'coming soon' hint.

### [minor] Poster-grid view header/search/empty copy still says 'songs'/'tracks', never 'films'  (kbSafe=True)
detail: In poster (Flixs) mode the view reuses #view-songs verbatim: the count reads 'N tracks' (static label, index.html:4527; refreshSongs only sets the number, jukebox.js:524), the search field placeholder reads 'Search songs or artists' (index.html:4531), and the empty state reads 'No songs match "q"' / 'No songs here yet.' (jukebox.js:540-543). For the AI Flixs section these all mislabel films as songs/tracks. The hero card itself is well-branded ('N films ?'), so the wording regresses the moment you enter the grid.
fix: When songsPoster is true, relabel: count ? 'N films', search placeholder ? 'Search films', empty text ? 'No films match?'. Drive off songsScope/songsPoster in openSongs/refreshSongs.

### [major] The advertised "OR ? Use Bonus Sparks" path is mathematically unreachable through play  (kbSafe=True)
detail: The showroom and grid prominently sell a second currency: every cosmetic shows real price, then "OR", then a gold Bonus-Sparks price + a "Use Bonus" button (index.html footHtml ~7994-7999, renderHero ~8091-8095), and the store legend (index.html ~4976) reassures "Bonus Sparks ? earned just by playing." But the economy makes that promise hollow. bonusPriceFor uses BONUS_RATE=30 (catalog.js consumed at index.html 7791), so the CHEAPEST 50-Spark guitar costs 1,500 Bonus Sparks and an 80-Spark premium costs 2,400. Meanwhile the faucet is 1-8 Bonus/run (S+full-combo=8; catalog.js 1294-1295) hard-capped at BONUS_WEEKLY_CAP=50/week (catalog.js 170). A perfect grinder therefore needs ~30 weeks to afford the cheapest guitar via Bonus, ~48 weeks for a premium. A new browsing player starts at 0 Bonus (loadBonusSparks default 0), so the gold "Use Bonus" button they see is, in practice, decorative ? which reads as a bait toward the real-money Sparks line. This is the one thing that makes the dual pricing feel slightly predatory rather than generous. (Context: this is partly an owner-locked decision ? Bonus spends on the website is the real revenue, so the in-game faucet was deliberately throttled ? and RELEASE_ROADMAP.md explicitly lists "wire dual-pricing OR hide the earn line" as an open call, so this is a known tension, not a regression.)
fix: Make the choice honest one way or the other: either (a) lower BONUS_RATE so a cheap guitar is reachable in a few focused weeks (e.g. rate ~8-10 ? 400-500 Bonus = ~2-3 weeks at cap), or (b) if the website-economy throttle must stay, stop foregrounding the Bonus price as a co-equal "OR" on guitars and instead show it as a small "or 1,500 Bonus Sparks" subline so it doesn't read as a real alternative. At minimum, surface the player's current Bonus balance and weekly-remaining (bonusWeekRemaining exists, catalog.js 179) inside the showroom so the gap is transparent rather than implied.

### [minor] "Use Bonus" spends instantly with no confirm, while real-Sparks Buy is two-click armed  (kbSafe=True)
detail: The real-Sparks Buy is deliberately a two-click "are-you-sure": first click arms a gold "Confirm ? N?" state with a 4s disarm timeout (buy(), index.html 8140-8151), plus a per-intent idempotency key against double-charge. The Bonus path (buyBonus(), index.html 8204-8225 ? catalog.js bonusBuy 200-209) has none of that ? a single click on "Use Bonus" immediately debits and equips. Bonus is non-cashable so this isn't a money-loss footgun, but it's an inconsistency: a misclick instantly burns a currency that (per the critique above) takes weeks to earn, and the guitar then equips with no undo prompt. The asymmetry also subtly trains the eye that the gold button is the "safe/cheap" one to mash.
fix: Give "Use Bonus" the same one-shot arm/confirm pattern as buy() (reuse _armedBuy / _disarmBuy), or at least a brief confirm given how scarce Bonus is. Cheap, keeps the two spend paths feeling equally deliberate.

### [nit] Showroom info column has an entrance-timing gap (8 children, 7 stagger delays)  (kbSafe=True)
detail: .sh-info contains 8 children ? eyebrow, name(h2), rule(hr), lore(p), traits, pricebox, actions, reassure (markup index.html 4932-4940) ? but the staggered shFade entrance only defines :nth-child(1)..(7) delays (index.html 1171-1177). The 8th child, .sh-reassure, falls through to the base rule (#store-hero.show .sh-info > * { animation: shFade ... } at 1170) with delay 0, so it fades in at scrim-time, BEFORE its visual predecessors finish cascading. It's usually empty (owned items clear it; only a guest viewing an unowned item gets "SIGN IN ON REACTIVVIBE TO SPEND SPARKS", renderHero 8096), so it mostly doesn't show ? but for a guest it pops in out of order at the bottom of the panel.
fix: Add a :nth-child(8) { animation-delay:.52s } rule, or scope the stagger to the elements that actually animate. One CSS line.

### [nit] "COMING SOON" showroom is a soft dead-end with no next action  (kbSafe=True)
detail: If a not-yet-finished item reaches the hero, renderHero's soon branch (index.html 8086) renders only a "COMING SOON" pill and wipes .sh-actions + .sh-reassure ? no "notify me", no "why", no alternative. The player has clicked into a full showroom and lands on a blank-bottomed panel whose only exit is X / Esc / backdrop. In practice storeFinished() + the grid filter (storePlayable, render() 8030) keep unfinished SKINS out, so today this mainly affects any non-skin level the STORE_FINISHED_NONSKIN allowlist hasn't enabled ? low surface area ? but it's still an unsatisfying terminal state for a click that promised a product page.
fix: In the soon branch, keep the lore/traits and add a single line like "Dropping soon ? check back" plus a clear close affordance, so the showroom never bottoms out on a lone pill.

### [minor] Guest sees a real-Sparks price + "Sign in" but a working Bonus buy they have no balance for  (kbSafe=True)
detail: For a signed-out browser, the grid/hero render the real price with a "Sign in" button (buyBtnHtml 7980) AND the gold "Use Bonus" button, which actually functions client-side without sign-in (buyBonus/bonusBuy never check signedIn). A fresh guest has 0 Bonus (catalog.js 148-151) and can't earn any without playing, so clicking "Use Bonus" just toasts "Not enough Bonus Sparks ? earn more by playing" (8223). The net first impression for a guest in the showroom is two priced paths, both of which dead-end (one needs sign-in, one needs a balance that doesn't exist yet) ? slightly deflating for the exact moment you want them to feel they CAN get the guitar.
fix: For guests, either hide the Bonus line entirely (it's unreachable for them) or replace its button with a "Play to earn Bonus Sparks" nudge that closes the store and starts a song, turning the dead-end into an on-ramp.

### [major] Passing your own BEST mid-song is a silent text swap ? the loop's biggest dopamine beat has no juice  (kbSafe=True)
detail: game.js:3872 ? when the live score crosses the stored best, the on-highway chip flips text to '? BEAT BEST' and recolors crimson via the .beaten class (index.html:2037). That is the ENTIRE celebration. Compare the surrounding code: a combo milestone (game.js:3095-3108) fires emitComboWave + lightningT + comboGlow + scanT + cameraShake + bgPulse + _scorePop + navigator.vibrate + a 'STREAK' flashJudgment; OD activation (game.js:2861-2868) fires odBurst + cameraShake + _scorePop(true) + flashJudgment('OVERDRIVE') + haptics + SFX. For a returning player, eclipsing your own record is THE payoff of the whole replay, and right now it is quieter than landing a routine 25-combo. The dopamine spike the persona came back for never fires ? it's a corner-chip relabel they'll often miss while reading notes.
fix: Make the BEAT-BEST crossing a real moment at the line-3872 flip: call _scorePop(true), a flashJudgment('NEW RECORD','#e0a93f') (gold, on-brand), a one-shot cameraShake + bgPulse, a navigator.vibrate, and a short gold edge-flash (reuse the odBurst pattern at lower intensity). The existing !classList.contains('beaten') guard already makes it fire exactly once. Keep it cosmetic so score/leaderboard math is untouched.

### [minor] Desktop HUD Overdrive readout is the player's main OD cue and it just says 'CHARGING ? NN%' ? the meter fills with no sense of HOW you fill it  (kbSafe=True)
detail: game.js:3898 drives #hud-od-text through CHARGING?NN% ? '? READY ? PRESS SPACE' ? 'OVERDRIVE ACTIVE!'. The fill comes from per-hit charge (+0.022/note, +0.14 on a star note; game.js:3146) ? roughly ~45 clean notes from empty. A returning player optimizing their run gets a creeping percentage with no signal that stars and unbroken streaks feed it, and no near-full anticipation beat before READY. The on-highway READY banner (game.js:4698) is great once full, but the long CHARGING ramp before it is informationally flat. (Note: the desktop readout hardcodes 'PRESS SPACE' ? correct for the keyboard owner, but inconsistent with the build89 on-highway touch fix at 4729 that adapts to 'TAP THE FLAME'. Keyboard-safe for this owner; flag only for the touch surface.)
fix: Add a single near-full state to the readout text (e.g. 'ALMOST ? NN%' at >=85%) and a subtle fill-bar glow ramp as overdrive?1 so the meter telegraphs the impending READY. Optionally surface that stars charge faster (a tiny '+OD' tick on star-note hits). Mirror the has-touch wording fix from line 4729 onto the line-3898 readout for consistency.

### [minor] The results 'beat it by N' chase CTA is plain grey body text under a histogram ? weak forward-pull into the next replay  (kbSafe=True)
detail: catalog.js:1273-1280 paints the next-chase CTA ('Best 1,234,567 ? beat it by 45,210') only when you did NOT beat the best, anchored BELOW the timing histogram (build86) as 12px grey Chakra-Petch with the numbers in gold/crimson. It's the literal hook that should make the persona hit Replay (Enter), but visually it reads as a footnote, not a gauntlet. The replay-keyboard flow itself is solid (game.js:2912 Enter=replay, Esc=menu), so the path is there ? the CTA just under-sells the dare.
fix: Promote the chase line into a small framed 'NEXT TARGET' card (crimson hairline, the gap number large in Oxanium) placed nearer the score/badges, and pair it with the existing Replay affordance (e.g. 'Replay ? beat by 45,210'). Pure results-screen markup/CSS ? no scoring or keyboard impact.

### [minor] Score multiplier hard-caps at 4x by ~combo 36 (classic feel), so the back half of a clean run has a flat ceiling with no score-rate left to chase  (kbSafe=True)
detail: timingProf classic = {comboStep:12, comboCap:3} (game.js:74); curMult/HUD cap at MAX_MULT=4 with OD's +1 tier (game.js:2710, 3126, 3885). A clean classic run hits the 4x ceiling around combo 36 (3 base tiers + OD) and stays there the rest of the song. After that, only the raw combo counter and the cosmetic tier ladder (HOT/BLAZE/GOLDEN at 25/75/150...) keep climbing ? score-per-note stops accelerating very early, so a 400-combo stretch feels mechanically identical to a 40-combo stretch. This is intentional and leaderboard-safe (the notes_total*1500 ceiling depends on it), but for a score-chaser the multiplier ? usually the core tension of a GH run ? goes quiet two-thirds early.
fix: Don't touch the score ceiling (leaderboard-locked). Instead lean harder on making the cosmetic late-run tiers FEEL like escalation: ensure GOLDEN/INFERNO/ASCENDANT crossings get progressively bigger one-shot moments (onComboTierUp partially does this), and surface a visible 'MAX' lock state on the mult badge so the player understands the 4x is the cap and the streak itself is now the score (every note at 4x = +1500). Communication, not a math change.

### [nit] Early-release hold 'DROPPED' breaks combo as hard as a miss, but the 0.75 grace boundary is invisible  (kbSafe=True)
detail: endHoldEarly (game.js:2728-2748): releasing a hold before holdScored>=0.75 sets dropped, zeroes combo, drains stability, deadens the lane, flashes 'DROPPED'. Past 0.75 it silently completes ('HOLD!'). The grace threshold is a hard cliff with no in-run telegraph of where it sits on the sustain beam, so a returning player optimizing a full-combo eats surprise combo breaks on holds they thought they'd banked ? and a hold-drop is as combo-fatal as a flat miss, which stings disproportionately for a timing they couldn't see.
fix: Render a 'safe to release' marker on the hold beam at the 0.75 point (a gold notch / segment-fill) so the player can SEE when the sustain is banked. Optionally soften the pre-grace drop from full combo-zero to a combo-knockdown if GH-feel tuning allows ? but at minimum make the grace boundary visible.

### [major] AI FLIXS Watch overlay has no Escape key and no focus management ? the lone modal in the app that breaks keyboard-close parity  (kbSafe=False)
detail: openWatch() (catalog.js:1120-1138) builds #flix-watch with only a ?-click and scrim-click close (lines 1129-1130). There is NO Escape handler. Every OTHER overlay in the app closes on Escape ? sheet, store, store-hero (index.html:8123, capture-phase), settings/how-to (8958/8975), results, calibration (9174), splash (6297). The owner plays KEYBOARD, so a keyboard user who opens a Watch preview is stuck reaching for the mouse to dismiss it. It is also not a real modal: no role="dialog", no aria-modal, no focus trap, no focus-restore ? whereas its build88-sibling, the store showroom hero, got exactly that treatment in builds 89/90 (Escape guard + #store-grid.inert focus-trap + teardown). So the two newest modals were finished to different standards. Not a flow-breaker (?/scrim work and the muted preview is off the critical path), hence a critique not a blocker.
fix: Add a capture-phase Escape listener that closes #flix-watch when .open (mirror the store-hero pattern at index.html:8123), move focus to .fw-close on open, restore focus to the originating card on close, and add role="dialog" aria-modal="true" to the .fw-box.

### [minor] Motion language is fragmented: build91 shipped shared easing tokens but only wired them into the store hero ? three different hand-tuned 'arrival' curves still coexist across the primary surfaces  (kbSafe=True)
detail: build91 added --ease-settle/spring/bar to :root (index.html:49-51) and re-pointed only the store-hero entrance to them. Everywhere else the same 'settle/spring' shapes are still hardcoded as literal ? and not even consistently: menu-hub card cardIn = (.2,1,.3,1) [3143] (numerically IS --ease-settle but hardcoded); menu-hub TILES mhTileIn = (0.18,0.9,0.26,1) [3191] and the start lockup matches it [3206] ? a DIFFERENT curve; coverflow coverLand/stage = (.25,.46,.45,.94) [jukebox.css:135,141] ? a THIRD curve; results-bonus badgePop = (.2,1.4,.3,1) [index.html:2298] which IS --ease-spring, hardcoded. The most-seen surface (coverflow) and the title/hub entrances each arrive on a slightly different cadence than the store. The CHANGELOG (build91) itself flags 'the full 15-site cubic-bezier swap is deferred' ? this is that debt, visible as a subtle motion-cohesion seam.
fix: Finish the token swap: replace the literal beziers in cardIn/mhTileIn/coverLand/start-lockup/badgePop with var(--ease-settle|spring|bar). Pick ONE canonical arrival curve for entrances (the tiles' (0.18,0.9,0.26,1) and settle's (.2,1,.3,1) are close but not identical ? collapse to one) so every surface lands with the same house cadence.

### [minor] AI FLIXS poster card uses a different type pairing than every other media item in the app ? Chakra Petch title/sub vs. the established Unbounded title + JetBrains Mono sub  (kbSafe=True)
detail: The new video-card (jukebox.css:495-496) renders .vc-title and .vc-sub both in 'Chakra Petch'. But the music song-card ? sitting in the SAME Browse?list view ? uses .sc-title = 'Unbounded' and .sc-sub = 'JetBrains Mono' (index.html:2767-2768). Same role (media-item title/subtitle), two different type systems, side by side when you flip from a music genre to AI FLIXS. Every other media title in the app (coverflow .jb-title, results, store .sh-name) is Unbounded; the FLIXS card silently drops it, so the poster grid reads as authored by a different hand. Minor because it's only the FLIXS surface, but it's a concrete cohesion seam in a build88 deliverable.
fix: Set .vc-title to 'Unbounded' (weight 600, matching .sc-title) and .vc-sub to 'JetBrains Mono' (matching .sc-sub) so the poster card inherits the same media-item type ladder as the song row.

### [nit] 'AI FLIXS' wordmark is rendered in gold ? the clearest stretch of the gold = owned/achievement/OD charter among the evolved surfaces  (kbSafe=True)
detail: .fh-badge paints the 'AI FLIXS' section label in var(--gold) with a gold glow (jukebox.css:394-395). The brand law reserves gold for owned/equipped/achievement/OD ONLY, and the rest of the app is disciplined about it: owned-line, Bonus-Sparks price, S-grade, results stars, encore chant, OD bar/edge are all gold for an EARNED/OWNED reason; the HUD multiplier even comments 'no gold here'. AI FLIXS is a content-category nav label, not an owned/achievement context, so this widens gold into section-branding and slightly dilutes the 'gold means you earned/own this' signal. Defensible as a 'premium content' cinematic accent, but it's the one place gold is used for navigation.
fix: Either accept it as a deliberate premium-content exception and note it in CLAUDE.md (like the green-fret exception), or render the AI FLIXS wordmark in chrome/crimson and keep the gold glow as a subtle surround so the category still feels premium without claiming the 'owned' color.

### [nit] FLIXS poster cards always show a 'Soon' badge with a hover ? play glyph ? a small mixed signal on the discovery surface  (kbSafe=True)
detail: videoCard() hard-codes a 'Soon' badge on every poster (jukebox.js:508) AND renders a crimson ? vc-play button that blooms on hover (jukebox.css:485-488), while the card actually opens a sheet whose CTA is '? Watch' (or 'Coming soon' if no source). So a single card simultaneously signals 'Soon' (not playable), a prominent play-triangle (looks playable), and then resolves to 'Watch a preview'. The intent (discovery-only, Phase 6 owns real playability) is correct and the boundary is well-guarded in code, but the on-card iconography sends three slightly different promises before the click.
fix: Make the hover affordance read as 'preview' not 'play' on poster cards ? e.g. swap the ? bloom for an 'eye'/'Watch' glyph, or only show ? when videoWatchUrl() exists; keep 'Soon' for the unplayable-into-engine truth so the two cues stop competing.

### [minor] AI FLIXS "Watch" preview overlay cannot be closed with the keyboard (Escape)  (kbSafe=False)
detail: openWatch() (catalog.js:1120) builds the #flix-watch overlay and wires close ONLY to the ? button click and scrim click (lines 1129-1130). There is no Escape handler for #flix-watch anywhere ? every other overlay in the app (store hero index.html:8123, store/levels/leaderboard/profile/settings/howto) has a per-screen Escape, but this new Phase-5 overlay was added without one. The global Escape routers (index.html:8957, 8974) only know about the named overlay screens, not #flix-watch, so they never close it. Worse, the overlay contains a <video controls> element which grabs keyboard focus, and Escape on a focused <video> does nothing ? so a keyboard user who clicks Watch is stuck reaching for the mouse to dismiss. The owner plays keyboard. It is NOT a flow-breaker (the ? and the scrim both work, and the overlay is a dead-end-free preview), so it is a critique, not a blocker.
fix: Add a capture-phase Escape handler in openWatch() (or the wireHeroOnce-style once block) that calls closeWatch() when #flix-watch.open ? mirror the store-hero pattern at index.html:8123. Also focus the .fw-close button on open so the first Escape/Tab lands on a closable control rather than the video element.

### [minor] AI FLIXS hero count and "Soon" badges depend on a backend video bucket that does not exist yet ? mock/empty catalog silently hides the whole surface  (kbSafe=True)
detail: videoCount()/videoTracks() derive from catalogVideos, which is filled by isVideo() ? a FUZZY title/genre heuristic (catalog.js:790-805), since Lovable has not shipped a media_type column yet (acknowledged swap-seam). On the documented HTTP-500 / empty-library degradation the game falls back to buildMockCatalog(1000) (catalog.js:754), which has no videos, so videoCount()===0 and the AI FLIXS hero tile is hidden entirely by the nVid>0 guard (jukebox.js:286). That is the correct graceful behavior (no broken hero, no dead-end), but it means the headline new feature of builds 88-92 will be INVISIBLE in exactly the fallback state the project is currently stuck in per the CHANGELOG (radio_tracks relationship missing). A playtester on the broken backend will never see AI FLIXS and may report it 'missing'.
fix: No code change needed for readiness ? this is expected. For the playtest, either confirm the live catalog returns video rows before the session, or temporarily seed a few heuristic-matching video entries into the mock catalog so the AI FLIXS surface is exercisable offline. Flag to the user that AI FLIXS visibility is gated on the live backend.

### [nit] Video posterFor() and videoWatchUrl() are best-effort fallback chains ? a video with only square art + an HLS-only source shows a stretched poster and a Watch that immediately punts to a new tab  (kbSafe=True)
detail: posterFor() (catalog.js:827) falls back poster_url?thumbnail_url?first_frame_url?artwork_url; the last is square cover art shown in a 16:9 frame (object-fit:cover crops it, acceptable). videoWatchUrl() (catalog.js:784) falls back video_url?stream_url?media_url; if the only source is .m3u8 (HLS), v.play() rejects and openWatch() does window.open(src,'_blank') (catalog.js:1137) ? i.e. the 'inline preview' silently becomes a raw new-tab .m3u8 the browser likely can't play either. The boundary is safe (never enters the engine) and there's a fallback, but the experience for an HLS-only film is poor. Pre-Phase-6 this is acceptable.
fix: When videoWatchUrl resolves to an .m3u8 (already detected for charting at catalog.js:777), consider keeping the Watch button but labeling/handling it as 'Open on ReactivVibe' rather than implying an inline preview, so the new-tab punt isn't a surprise.

## DELIGHTS

- True first-run on-ramp is genuinely thoughtful: a brand-new player (no rr_career/rr_scores) is auto-started on EASY (game.js:2623-2627) with a toast 'Starting you on EASY ? bump it up anytime', and Easy only uses 3 lanes (A/S/D), no bombs, slow 1.30 approach, and the widest 0.20 hit window ? a real, gentle ramp instead of dropping them into 5-lane Medium.
- The How-To overlay is unmissable but non-trapping: it auto-pops once after the title/intro settle, never over live gameplay/countdown/results (game.js:6029), and re-arms on a stray backdrop tap until the player explicitly acknowledges via GOT IT or CALIBRATE (game.js:6033-6036) ? so a first-timer can't accidentally dismiss the tutorial and be left guessing.
- Overdrive is taught AND cued clearly for keyboard: the how-to says 'Hit SPACE for a multiplier surge', the OD gauge label literally reads 'OVERDRIVE ? SPACE' (index.html:5309), and in-game the HUD walks CHARGING?N% -> '? READY ? PRESS SPACE' -> 'OVERDRIVE ACTIVE!' (game.js:3898) plus a one-shot 'OVERDRIVE READY' flash (game.js:3900). A new player will know exactly when and how to fire it.
- A real strike-line rail is drawn across the catcher row (game.js:4313-4337) ? crimson underglow + crisp chrome core that flashes hot-white on a hit ? giving a clear 'hit HERE when the note reaches this line' cue, which is the WHEN half of the answer and reads on any backdrop.
- Strong resilience for a newcomer when the backend is down: loadCatalog() falls back to a 1000-song mock catalog with an honest toast ('Couldn't reach the library ? showing samples', catalog.js:750), and the play router degrades to playDemo() (catalog.js:1111) so the player ALWAYS gets a playable run rather than a dead library or a stuck loading screen.
- Results screen reads cleanly for a first-timer: a big grade, star rating, and four labeled cells (Final Score / Accuracy / Max Combo / Notes Hit), plus a proportional Perfect/Great/Good/Miss composition bar with a counted legend (index.html:5424-5445) ? you instantly see the SHAPE of your run, not just a number. Keyboard Enter=Play again / Esc=Menu is spelled out on-screen.
- Empty-press 'whiff' is forgiving by design: a mistimed press with no note in window flashes a subtle '?' and explicitly does NOT break combo or drain stability (game.js:3055-3057), a deliberate keyboard-feel guardrail so a flailing newcomer isn't punished for early experimentation.
- Countdown is welcoming and paced: 'GET READY' beat -> 3 -> 2 -> 1 -> 'GO!' (game.js:2316-2333) with the words sized to fit, giving a newcomer a clear, unhurried moment to ready their hands before the first note.
- The 'coming soon vs broken' question is answered cleanly: a film's sheet hint reads 'AI Flixs film ? tap Watch for a preview. The playable video level is coming soon.' (with a source) or 'playable video is coming to the game soon.' (without), and the poster cards carry a hardcoded 'Soon' badge (never 'Playable'). The boundary is explicit in copy AND enforced in code ? isVideo guards at the play handler (catalog.js:1082) and launchTrack (catalog.js:1148) make it impossible to accidentally launch a film into the rhythm engine. The player never hits a crash or a dead chart; it reads as 'intentionally coming soon,' exactly the intent.
- The AI FLIXS hero entry is genuinely cinematic and on-brand: full-width grid-column:1/-1 card above the genres, gold Unbounded 'AI FLIXS' wordmark with a soft glow, 'MUSIC VIDEOS & AI FILMS' eyebrow, and a live 'N films ?' count ? warm-dark body, crimson radial glow, chrome edge, zero purple/blue (jukebox.css:383-398). It clearly reads as its own destination, not just another genre tile.
- The poster grid itself is well-crafted: true 16:9 frames (aspect-ratio:16/9), cover-fit posters with a graceful ?-glyph fallback on image error (vc-noart), a bottom scrim for title legibility, duration chip, and a hover ? that scales in ? responsive auto-fill grid that drops to 2-col on mobile (jukebox.css:464-497). It feels like a streaming-service shelf, which is the right mental model for films.
- The build90 regression fix is solid engineering: tagging the Watch handler _preview=true so the env-picker wrapper skips applyEnvironment/clearEnvironment (index.html:8686) means tapping Watch no longer churns the menu backdrop or re-rolls the random stage ? a subtle side-effect that was caught and cleanly fixed. The HLS/autoplay-blocked path also degrades to a new-tab open rather than a silent failure (catalog.js:1137).
- Cross-media search is wired so films are actually findable: the header search sources allMedia() with scope 'all' (jukebox.js:615, catalog.js:878), so a film title surfaces at all ? before Phase 5 videos were filtered out of search entirely. The intent (make AI Flixs discoverable by name) is met end-to-end.
- The showroom genuinely feels premium and makes you want the guitar: the uncropped guitar PNG floats and sways on a spotlit plinth (shIdleRot, -8px + ?0.6deg, 5.5s loop, index.html 1167), a masked reflection tracks the float (shReflectIdle 1169), and the spotlight slowly drifts ?9px (shSpotDrift 1166). Skins float+sway (3D feel) while flat level cover art floats-only with no spin ? a smart, deliberate choice (comment at 1168: spin 'reads cheap' on flat art).
- Every motion is correctly gated behind html.rr-reduce-motion (1178-1183), and the whole showroom is an overlay that only opens from the library header (#store-open, 4456) ? never during gameplay and never focused while playing ? so the keyboard player's run is byte-identical. Accessibility and the no-FX-during-play rule are both respected.
- Real money is treated with real care: the Buy button is a two-click armed confirm showing the exact price ("Confirm ? 50?", buy() 8144-8151) with a 4s auto-disarm, plus a per-purchase idempotency key reused across retries (_buyKey 8136-8137) so a lost-response re-click dedupes server-side instead of double-charging. The buy-confirm is also cleared on grid rebuild and on store close (_disarmBuy in render() 8026 and close() 8270).
- Owned/equipped/coming-soon states are unambiguous and stay in sync live: cards carry rarity rings + gems, an OWNED tab, a gold EQUIPPED banner, and the hero re-renders itself after buy/equip (renderHero re-called at 8169/8219/7927) so clicking Buy flips to Equip without a reload. Buying a skin auto-equips it and toasts "Unlocked & equipped!" ? a satisfying single-step payoff.
- The currency model is presented transparently and ethically: a plain-language legend spells out real Sparks vs earned Bonus Sparks with "Purchases never pay-to-win" (4976), the two balances are shown side-by-side with distinct crimson/gold treatment, and the dual-currency intent (cosmetics buyable with earned currency, premium levels real-Sparks-only via bonusPriceFor returning 0 for non-skins) is principled. The free community guitar (Celine's Razor) correctly shows "Equip" with no price for everyone (FREE_ITEMS, catalog.js 191).
- Focus handling on the modal is done right: opening the hero sets #store-grid.inert=true so you can't tab to and buy a card behind the modal (8079), restores it on close (8103), focuses the close button on open, and returns focus to the previously-focused element on close (_heroPrevFocus, 8105). Escape uses a capture-phase listener so it closes just the hero, not the whole store (8123 + the store-level guard at 8278).
- The in-game BEST chip is wired correctly for the returning-player case: getBest (catalog.js:856) reads real rr_scores keyed trackId|difficulty and returns a genuine best even on LIVE catalog data (the catalogLive short-circuit at line 860 only skips the MOCK fallback, not real saves), and saveBest mutates the same _scoresCache reference it persists ? so on the very next replay in a session the chip already shows the freshly-set BEST. The thing to chase is real and live.
- build92's OD burn-down is a genuinely classy, brand-perfect touch: in the final 1.6s the edge-vignette quickens its pulse (period 140?~70ms) and lerps gold(255,180,90)?crimson(255,31,46) as odTimer?0 (game.js:4664-4677). The lerp stays in the red sector the whole way (never crosses into purple/blue), the composite stays 'screen' (additive, can't darken/muddy), and under reduce-motion the faster pulse is floored off but the static color shift is preserved ? so the wordless 'spend it NOW' countdown still reads for reduced-motion players. Textbook juice-within-constraints.
- NEW BEST handling is correct and clearly hardened by QA: the +delta is real (results.score - prevReal.score, catalog.js:1263), a first-ever best prints plain '? NEW BEST' instead of a confusing '+<entire score>' (build89 fix, line 1264), and the badge ordering is sound ? renderResults sets badges.innerHTML for Full Combo first (game.js:2538), then recordLocal runs AFTER and insertAdjacentHTML('afterbegin') prepends NEW BEST / Grade Up so they stack cleanly without clobbering each other.
- Keyboard feel is tight and footgun-free for the owner's input: OS key-repeat is explicitly ignored so only the first press judges and the held key drives sustain (game.js:2925), Space activates Overdrive but yields to a lane bind if Space was remapped (game.js:2921), the old Space-to-restart run-killer is gone, and window blur + document visibilitychange both release-all-lanes AND auto-pause (game.js:2934-2938) so a mid-hold focus loss can't strand a stuck key or silently tank a run. The score count-up lerps at dt*14 and snaps within 0.6 (game.js:4101-4102) ? responsive, never laggy.
- Combo milestones have well-layered escalation that keeps the 11-24 dead zones alive: full lightning STREAK every 25 (wave + scan + shake + _scorePop + 'STREAK!!' at every 100, game.js:3095-3108), a deliberate MID-STREAK pulse at the 15/40/65 halfway points (game.js:3113-3118) so there's a cue between the big payoffs, plus the named cosmetic tier ladder (HOT/BLAZE/GOLDEN/INFERNO/ASCENDANT) and an earned rainbow at combo>=150 or OD ? all cosmetic so the score ceiling stays leaderboard-safe.
- Single biggest visual WIN: the build92 Overdrive final-seconds burn-down (game.js:4659-4678). The edge-vignette quickens its pulse (period 140ms?~70ms) AND lerps gold?crimson as odTimer?0 ? a wordless, diegetic OD countdown that tells you the window is closing so you can spend it. It's brand-flawless: the lerp stays inside the red sector the whole way (gold ~33??crimson ~356?, never crossing toward purple/blue), composites with 'screen' so it can only add light, reads odTimer (never writes ? scoring untouched), and under reduce-motion it floors the faster pulse but KEEPS the color shift because that's information, not motion. This is exactly the 'juice that means something' the brand wants.
- Judgment color language is genuinely unified across HUD and results: in-game PERFECT=chrome #dad7d2 / Great=gold #e0a93f / Good=coral #ff6b78 / Miss=crimson #ff1f2e (game.js:85, 2746, 3674) maps 1:1 onto the results judge-bar segments (index.html:2009-2012). A player reads the same four colors in the moment and on the scorecard ? strong cross-surface cohesion.
- Brand tokens are warm-compliant and well-documented: :root darks are all R?G?B (#0a0706/#160c0b), --chrome is explicitly de-blued, and even the hairline --line is a muted CRIMSON (rgba(160,40,46,.32)) precisely so a light divider over the red glow can never blend to purple (index.html:35-47). The whiff dash was corrected from a cool #8a7f86 to warm #8a807c in build89. The team is actively defending the no-purple law at the token level.
- The store showroom (build84 + build91) is the most premium-feeling evolved surface and its gold use is correct: gold appears only on the eyebrow, the owned-line, the Bonus-Sparks price, and a hairline rule ? all genuinely premium/owned contexts. The build91 'comes alive' choreography is tasteful: arrive (shRise) ? settle ? breathe, with 3D guitars getting an ~1.2? sway but flat cover-art floating WITHOUT spin ('a spinning card reads cheap' ? exactly right), the reflection tracking the float, and reduce-motion fully restoring base transforms.
- The HUD multiplier strike-line (index.html:1837-1852) is disciplined: Oxanium (the brand number font), crimson?chrome with an explicit 'no gold here' comment, and a boosted state that scales+heats without breaking the palette. It correctly treats gold as off-limits in a non-owned/non-OD context ? the kind of restraint that keeps gold meaningful everywhere else.
- The Phase-6 'videos are not playable yet' boundary is defended at FOUR independent points, so no path can leak a video into the rhythm engine: the sheet play handler (catalog.js:1082 isVideo guard), launchTrack() used by the Levels picker (catalog.js:1148 returns false), the /play?trackId=<videoId> deep-link (catalog.js:1467 routes through openSheet which branches on isVideo ? shows Watch, never auto-launches), and the per-source readiness checks. A shared video deep link degrades to the Watch preview instead of dead-ending or crashing ? genuinely robust.
- The store-hero ? store ? global Escape priority is layered correctly across builds 88-90: the hero's capture-phase Escape (index.html:8123) wins over the store-screen Escape (guarded by !hero.show, 8278) which wins over the global router (bails when store-screen is active, 8961). First Esc closes just the hero, second Esc closes the store. The build90 inert-leak fix is complete ? store close() tears down an open hero (8270) and open() resets #store-grid.inert (8269), so the grid can never get stuck non-interactive on reopen.
- The build90 _preview tag is a clean, surgical fix for the env-side-effect regression: openWatch's handler is tagged fn._preview=true (catalog.js:1061) and the setMenuPlayHandler env wrapper skips applyEnvironment/clearEnvironment for preview handlers (index.html:8686), so clicking Watch no longer churns the menu backdrop or re-rolls the random stage ? while real engine launches (no tag) still stage the chosen environment exactly as before.
- The build92 OD burn-down cue is cosmetic-only and brand-safe by construction: odTimer is read, never written (scoring/duration/Space-activation untouched), the gold?crimson lerp pins the R channel at 255 the whole way (255,180,90?255,31,46 ? never crosses toward blue/purple), composite stays 'screen' (additive, can't darken), and under reduce-motion only the faster PULSE is floored while the static color countdown is preserved. It also survives FX-preset swaps via the base-only JUICE.odWarnAt key. node --check passes on all four edited JS files.
- openSongs() fully resets songsPoster, songsScope, and the #view-songs.poster-grid class on every call (jukebox.js:412-421), so the poster-grid layout never leaks back into a music list ? navigating Flixs ? genre ? search all switch modes cleanly. Music lists stay video-free and the Flixs list stays music-free via belt-and-suspenders scope filters (currentSongs, jukebox.js:428-429), while the header search opens scope 'all' so a video title is finally findable.
- Catalog degradation is graceful and signposted: a 200-but-empty library and a fetch failure both toast the player ('showing samples', catalog.js:745/750) and fall back to the mock catalog, and BOTH fallback paths still run the video split ? so the menu always has playable music and the new AI FLIXS plumbing never throws on a missing video bucket.