# Phase 4 plan + design-evolution DO-NOW batch (build90 / workflow wbqaae226)

Phase 4 winner: shadow-chart  scores: [{'key': 'shadow-chart', 'overall': 9, 'kbSafety': 10}, {'key': 'state-stream', 'overall': 8, 'kbSafety': 9}, {'key': 'minimal-intercept', 'overall': 8, 'kbSafety': 10}]
Design items: 25, DO-NOW: 14


## DO-NOW design items


### 1. Overdrive final-2s burn-down cue (gold?crimson cool + faster pulse + descending tick)  [S/high]  kbSafe=True brandSafe=True
FILE: game.js render() ? the `if (odActive)` gold-vignette block at lines 4661-4670 (edge color hardcoded `rgba(255,180,90,a)` at 4666; pulse is the fixed `0.5+0.5*Math.sin(performance.now()/140)` at 4662, NOT odTimer-driven). Drain happens at 4108-4111 where `odTimer<=0` flips odActive=false + fires the single `emitFx('odend')` shockwave with zero anticipation. `odTimer`/`OD_DURATION` live at 118-119/2847.
STEP: Inside the existing `if (odActive)` block (4661), when `odTimer < 1.6`: (1) scale the pulse frequency by `(1 + (1.6 - odTimer))` so the vignette breathes faster as it empties; (2) lerp the edge stop at 4666 from gold (255,180,90) toward crimson (255,80,70) by `t=(1.6-odTimer)/1.6` so the frame visibly cools ? a wordless countdown, still warm/additive `screen` comp so it never reads purple. Optionally add an inward burn-down ring mirroring the odBurst ring at 4681-4687. For audio, in the drain block (~4108) fire one soft descending tick at the ~1.5s mark reusing the existing AC synth fallback path, guarded by `!muted`. Add a `JUICE.odWarnAt` field to the JUICE object (line 127) + presets (129-131) so it's live-tunable via __rrJuice. Cosmetic only ? never touches odTimer/scoring; Space remains the only activator. Floor the pulse-ramp under reduceMotion (the block already reads reduceMotion at 4662). Bump ?v, node --check.

### 2. Perfect-only catcher signature ? hairline white-core stroked snap ring  [S/med]  kbSafe=True brandSafe=True
FILE: game.js spawnHitParticles() at 3695. `isPerfect` is set at 3698; perfects already get TWO extra FILLED rings (3711-3712) that read as volume, not a distinct snap. `reduced = fxLite || reduceMotion` gate at 3703. laneHitPulse[lane] is set to 1.0 on hit (3145) and decays at 4021.
STEP: In spawnHitParticles, in the `if (isPerfect && !reduced)` branch (3710-3713), push ONE ultra-short STROKED (not filled) hairline ring: `{ring:true, stroke:true, x:laneX, y:hitY, age:0, life:0.07, color:'255,255,255', max:lw*0.55}` ? a categorical 'click' snap at the catcher distinct from the soft filled shockwaves. If the particle renderer has no stroked-ring path, add a thin `stroke` flag branch in the ring draw (low effort, additive lighter comp). Pair with a 1-frame catcher-rim white flash by bumping the white component of laneHitPulse[lane] for one frame. Keep it tiny, at the catcher only ? NO center-screen flare (explicitly removed in build19/20 per the code doctrine 'energy only where play happens'). White core is on-brand chrome/white-hot. No timing/scoring change. Bump ?v, node --check.

### 3. Approach-to-milestone tension band ? brighten the last ?4 notes into each combo%25 lightning  [M/high]  kbSafe=True brandSafe=True
FILE: game.js handleHit milestone logic: full LIGHTNING at `combo % 25 === 0` (3095) sets comboGlow=1 (3099); mid-pulse at `combo % 25 === 15` (3113) sets comboMidT=0.34 (3114). comboGlow decays at 4033, comboMidT at 4052. The comboMidT screen-band gradient is drawn at 4595-4606; lightning bolts at 4606+. No pre-milestone wind-up exists ? payoff arrives with no crescendo.
STEP: Declare a new `let comboApproachT = 0;` near comboMidT (222) and decay it in loop alongside comboMidT (~4052). In handleHit, after the milestone/mid blocks (~3115), compute `const toGo = 25 - (combo % 25);` and when `toGo <= 4 && combo >= 21` set `comboApproachT = (5 - toGo) / 5` (brightens each successive note toward the milestone). In render, near the comboMidT band (4595), draw a thin warm gold/crimson edge-glow at low alpha scaled by comboApproachT so the screen literally tightens right before the lightning lands; optionally nudge comboGlow up a touch per approach note so the strings pre-heat. Cap intensity below the comboMidT band so it never competes with note-read clarity (the burst code already guards the read-path). Cosmetic only ? must NOT change mult/score. Skip under `fxLite`/`reduceMotion` like sibling FX. Bump ?v, node --check.

### 4. Key glyph baked onto catcher rings (first ~7s / first 3 runs, fades out)  [M/high]  kbSafe=True brandSafe=True
FILE: game.js ? render() catcher loop at L4340-4362 (right after the drawCatcher(nearX[i], cY, ...) call at L4348). Reuse keyForLane(lane) (L47) + keyGlyph(k) (L5683). Add a first-N-runs gate reading the same signal _firstRunEasy uses (!localStorage.rr_career && !localStorage.rr_scores, L2623). Reset the fade timer in resetScoring() (L2180).
STEP: Add an eased opacity term _catcherKeyHint (1.0 at song start ? 0 over ~7s of songTime; force 0 once history exists so veterans never see it). When >0.02, after the drawCatcher call at L4348: ctx.save(); set font Oxanium ~lw*0.16, fillStyle rgba(220,215,210,a) (warm chrome, R>=G>=B ? NOT blue), textAlign/baseline center, fillText(keyGlyph(keyForLane(i)).toUpperCase(), nearX[i], cY); ctx.restore(). MUST read keyForLane(i) (the same source updateFooterHint/renderKeycaps use at L2151/L5705) so a remapped or GH-profile player sees their real key. Skip when document.body has 'has-touch' or a GH-strum profile is active. Gate behind reduceMotion/fxLite. Pure ctx.fillText on the render layer after the catcher draw ? touches no input/scoring path, so keyboard play stays byte-identical. Biggest first-90s gap: today the only key reference is the 11px footer hint strip, nowhere near the falling notes.

### 5. First-note 'PRESS' beacon ? guarantee the first-ever win  [M/high]  kbSafe=True brandSafe=True
FILE: game.js ? tag in buildNotes after the FINAL sort (the chart is finalized by L1415, then re-sorted at L1452/L1553/L1592; tag after the last one, right where __rrChartStats is built ~L1620). Draw in the note render loop at L4365-4503 (the per-note branch already computes nx,ny and has n in scope; clear-on-judge near the n.judged checks).
STEP: In buildNotes, only on a true first-ever song (gate on !localStorage.rr_career && !localStorage.rr_scores, the idiom at L2623), set notes[0]._firstHint = true AFTER the final notes.sort so it doesn't get reordered. Do NOT otherwise alter chart/timing ? buildNotes must stay byte-identical (notes are time-sorted and hit-detection early-breaks on that, L3051). In the note render loop (~L4503, beside the drawNote(nx,ny,...) call), when n._firstHint && !n.judged draw an extra eased ring at nx,ny scaled by a sin pulse plus a small 'PRESS' caption in warm chrome. Clear the flag the instant it is judged (in handleHit/judge). No SFX, no input change. Pairs with the catcher glyph: glyph says WHICH key, beacon says WHEN ? together they convert the first note from a guess into a guaranteed teaching win. Strictly one-note/one-time or it patronizes.

### 6. First-run Easy nudge ? persistent inline chip (replace the lone 2.6s toast)  [S/high]  kbSafe=True brandSafe=True
FILE: game.js ? _firstRunEasy is set at L2624; today its ONLY signal is a single showToast on a 2600ms setTimeout (L2627). Clear-point = the existing diff-btn click handler that already writes rr_diff (L2612-2617). index.html ? add a gold/chrome pill near the difficulty selector (.diff-grid / .diff-btn at CSS L312-313); reuse the existing .si-flag.featured gold-pill treatment at L1186.
STEP: Add a small inline badge by the difficulty control, shown only while _firstRunEasy && !localStorage.rr_diff ('You're on EASY ? bump it up anytime'). Keep the toast as a secondary cue but make the chip the durable signal. In the existing diff-btn click handler (L2612-2617) ? which already sets difficulty + writes rr_diff ? also hide/remove the chip so it auto-clears the moment the player touches difficulty and never nags a returner. Never re-show once rr_diff is ever set. Quiet gold/chrome label (reuse the .si-flag.featured gold gradient at index.html L1186 ? gold is sanctioned here as a non-alarming 'state' cue), never a modal (a modal would block the dopamine of just pressing play). The on-ramp is invisible today because the toast auto-dismisses while a new player drinks in the title art. Pure overlay/markup ? no input path touched.

### 7. Whiff cue teaches the lesson once ('NO NOTE ? WAIT FOR ONE')  [S/med]  kbSafe=True brandSafe=True
FILE: game.js ? empty-press branch at L3055-3061; the dash flash is L3057: flashJudgment('?', '#8a807c', true). flashJudgment signature is (text, color, isWhiff) at L3808; _lastRealJudgeMs at L3807. New one-time flag localStorage rr_whiff_taught.
STEP: In the whiff branch at L3057, gate: if first-run (no rr_career/rr_scores) && !localStorage.rr_whiff_taught ? flashJudgment('NO NOTE ? WAIT FOR ONE', '#b9b2ac', true) and set rr_whiff_taught='1'; else keep today's flashJudgment('?', '#8a807c', true). CRITICAL: keep passing isWhiff=true so the teach string still can't clobber a real callout AND doesn't stamp _lastRealJudgeMs (L3809). Leave playWhiffSfx() + catcherRecoil[lane] + the no-combo/no-stability guardrail (the whole point) byte-identical ? only the displayed string changes, once ever. Keep the caption short so it fits the judge-flash slot. Brand-neutral warm chrome (#b9b2ac is R>=G>=B), NOT crimson ? crimson would read as 'you failed', the wrong message for a penalty-free event. NOTE: this branch was already hardened in build89 for the A5 clobber-guard (the >30ms check at L3057) and the A7 warm-color fix (#8a807c) ? both already shipped, so this is purely the additive teach layer on top.

### 8. Hero showroom: slow idle float + spotlight drift (alive, not photographed)  [S/high]  kbSafe=True brandSafe=True
FILE: index.html ? CSS only on .sh-guitar (~line 1106), .sh-reflect, .sh-spot; extend the existing html.rr-reduce-motion kill block already present in the #store-hero rules. No JS. The hero markup/entrance is real: openStoreHero (index.html:8044) sets g.src on $h('.sh-guitar') and toggles .is-cover for cover-art items (8050).
STEP: Add @keyframes shIdle{0%,100%{transform:translateY(0)}50%{transform:translateY(-8px)}} and apply it as a SECOND animation on .sh-guitar that starts after the one-shot shRise entrance finishes (animation: shRise .5s <ease> , shIdle 5s ease-in-out .5s infinite). Add a slow translateX/rotate drift (~1.2deg max) to .sh-spot for spotlight sway, and mirror the float onto .sh-reflect inverted so the reflection tracks. Gate: for #store-hero.is-cover (cover-art, set at openStoreHero:8050) float but DO NOT rotate (a flat card spinning reads cheap). Add all three selectors to the existing html.rr-reduce-motion override so reduce-motion floors them. Keep amplitude tiny (6-10px / ~1.2deg) or it reads as wobble. Bump ?v. Overlay-only element that never has focus during play ? keyboard byte-identical.

### 9. Show Bonus-Sparks balance + 'almost there' nudge in the hero pricebox  [M/high]  kbSafe=True brandSafe=True
FILE: index.html ? renderHero() not-owned branch (8075-8082); read balance via RC().getBonusSparks() (the same RC() guard pattern used at setBonusBalance index.html:8167; getBonusSparks is real in catalog.js:154 and exported at catalog.js:1430). bonusPriceFor(it) at index.html:7777. New .sh-bonus-have CSS class near .sh-reassure styling.
STEP: In renderHero's else (not-owned) branch (8076-8081), after computing bp=bonusPriceFor(it) and building the 'price OR bonus' line, when bp>0 read have=(RC()&&RC().getBonusSparks)?RC().getBonusSparks():0 (mirror the guard at 8167). Append a small line under .sh-pricebox: if have>=bp render 'You have {fmt(have)} Bonus' in GOLD (gold is the sanctioned earned-currency color ? .sh-price.bonus already uses --gold); else render 'You have {fmt(have)} ? {fmt(bp-have)} more by playing' in --ink-dim NEVER red/crimson (a paywall-red 'can't afford' slap is off-brand and predatory). Add a .sh-bonus-have class (Chakra Petch ~11px). Bonus needs no sign-in (buyBonus at 8189 has no auth gate), so render regardless of signedIn. Overlay-only ? keyboard untouched.

### 10. Genuine 'unlocked!' / 'equipped' celebration beat on the hero stage after buy/equip  [M/high]  kbSafe=True brandSafe=True
FILE: index.html ? buy() success path (8146-8157, currently only setBalance+owns+equipSkin+toast+render+renderHero) and equipSkin (7906, only toast+render+renderHero); both fire ONLY from store click handlers, never the input router. Add a transient .sh-claimed element over .sh-stage and a celebrateHero(kind) helper.
STEP: Add an absolutely-positioned .sh-claimed element inside .sh-stage (z above .sh-guitar). Write celebrateHero(kind): if a hero is open (_heroItem truthy, index.html:8042/8068), add a class that triggers a one-shot ~700ms @keyframes ? gold radial ring-expand + plinth flare (reuse --gold and the existing .sh-plinth glow) + a 'NOW YOURS'/'EQUIPPED' wordmark fade ? and auto-remove on animationend. Call celebrateHero('bought') in buy()'s res.ok block (after line 8154 where it already calls renderHero(_heroItem)) and celebrateHero('equipped') in equipSkin after its renderHero(_heroItem) at 7912. GOLD only ? gold is the brand's reserved owned/equipped signal; NO crimson confetti (crimson is action). Under html.rr-reduce-motion swap the burst for an instant static stamp (opacity 0?1?hold). Keep it non-blocking ? never trap focus or delay the Equip button. Fires only from store clicks ? keyboard byte-identical.

### 11. Shared easing tokens in :root (--ease-settle / --ease-spring / --ease-bar)  [S/high]  kbSafe=True brandSafe=True
FILE: index.html ? add to :root token block (lines 32-48, after --line at 48); then swap the copy-pasted cubic-bezier literals across the file
STEP: Add `--ease-settle: cubic-bezier(.2,1,.3,1); --ease-spring: cubic-bezier(.2,1.4,.3,1); --ease-bar: cubic-bezier(.2,.8,.2,1);` to the :root block (index.html:32-48). Then replace the literal `.2,1,.3,1` settle curve with var(--ease-settle) at its real sites ? NOT just the 5 the QA2 doc names. Grep confirms it is FAR more widespread than claimed: cardIn/profile-card (105), profile-guitar transition (605), rs-card (747), lbd-card (842), rcDeath/rcWorld (812-813), store-card (1019), shRise/shPlinth/shFade (1155-1158), menu-hub mh-card (3128), multiplayer mp-card (3291), encore (2255), rb-bar seg (2304) ? 15+ sites. Swap the (.2,.8,.2,1) bar curve (710, 1214, 733, 3153, 3207, 3229) to var(--ease-bar), and badgePop's (.2,1.4,.3,1) (2269, 2273-2274, 2283) to var(--ease-spring). Re-point the countdown `.count` keyframe (index.html:1460 `animation: count 1s ease-out forwards`) to var(--ease-settle) so the 3-2-1 shares the house arrival. JS untouched. Bump ?v from 327. Verify: grep `cubic-bezier` after the swap to catch strays; reduce-motion stays honored via the global damp at index.html:51-56.

### 12. Re-skin results timing + chase blocks to brand color tokens (--ink-dim / --chrome)  [S/high]  kbSafe=True brandSafe=True
FILE: game.js:2522,2524,2526 (timing histogram inline HTML) + catalog.js:1277 (chase CTA inline HTML); optionally lift into .rr-timing/.rr-chase classes in index.html near .results-badges
STEP: The two newest results blocks carry hardcoded greys that sit a half-shade COOL/off vs --ink-dim:#a0938f (the deliberately WCAG-tuned ~4.7:1 warm-dim, index.html:38) and --chrome:#dad7d2 (index.html:39). Confirmed live: game.js:2524 `color:#b9b2ac`, game.js:2526 `color:#8a847d`, game.js:2522 histogram bar `rgba(236,231,227,0.45)`; catalog.js:1277 chase `color:#b9b2ac`. Replace #b9b2ac and #8a847d with var(--ink-dim); replace the rgba(236,231,227,0.45) non-on-beat bar fill with a --chrome-based class. KEEP the on-beat bar literal `#e0a93f` (game.js:2522, equals --gold; canvas/inline-string code cannot read CSS vars so the literal is correct there). Best form: move both inline blobs into real .rr-timing/.rr-chase CSS classes so they stop carrying inline hex and re-inherit the documented contrast tuning. node --check game.js after (string-built template literals). Render-only, no input path. Bump ?v.

### 13. Consolidate gold-pulse keyframes ? but split crimson-dim odpulse from pure-gold arm pulses  [S/med]  kbSafe=True brandSafe=True
FILE: index.html ? keyframes storeArmPulse (1089), vsodpulse (1536), odpulse (1810); consumers at 1088/1533/1805/1845 plus odpulse's wider users
STEP: FLAG a real subtlety the QA2 idea glosses: the three 'gold ready' pulses are NOT all gold. storeArmPulse (1089) and vsodpulse (1536) breathe pure gold?bright-gold, but odpulse (1810) starts at `rgba(255,60,40,0.4)` (R255 G60 B40 = crimson/red-orange) and warms to gold ? a crimson?gold breathe. The doc says 'use odpulse values as reference' for ALL three; doing so would tint the buy-confirm and versus-OD pulses crimson, changing their identity (and crimson on a buy-confirm reads as 'danger/cancel', not 'gold = armed/owned'). Safer plan: define ONE canonical pure-gold `@keyframes goldReady` (storeArmPulse's gold values: 0%/100% box-shadow gold .45, 50% gold .85) and re-point storeArmPulse (1088) + vsodpulse (1533) + the gauge.od.ready arm states (1805/1845) to it, per-site duration only. Leave odpulse SEPARATE (its crimson-dim is an intentional OD identity), OR if unifying, rename it and keep its crimson-dim distinct. Also note odpulse has 6 consumers incl. non-OD ones (rbadge.best 2273, rbadge.gradeup 2274, keycap.listening 4661, pad-wizard-swatch 4720) ? wider blast radius than the doc's 3. Grep for removed keyframe names before deleting. All states correctly gold/owned-armed; stays in brand law. Bump ?v.

### 14. Hero showroom idle float + spotlight drift (premium feel, CSS-only)  [S/high]  kbSafe=True brandSafe=True
FILE: index.html ? .sh-guitar (1106), .sh-reflect (1108), .sh-spot (1102/1157), reduce-motion kill block (1166-1168)
STEP: Borderline-in-lens (store surface) but it is a pure-aesthetic brand-feel upgrade, overlay-only. The hero sits dead-still after its one-shot shRise (1155). Add `@keyframes shIdle{0%,100%{transform:translateY(0)}50%{transform:translateY(-8px)}}` and apply as a SECOND animation on .sh-guitar that starts after shRise (`animation: shRise .5s var(--ease-settle) both, shIdle 5s ease-in-out .5s infinite`). Mirror the inverse float onto .sh-reflect (it is `scaleY(-1)` flipped at 1108). Add a slow translateX drift to .sh-spot (1102). For cover-art heroes (#store-hero.is-cover, 1116) float but do NOT rotate (a flat card spinning looks cheap). CRITICAL: the existing reduce-motion kill block (1166-1168) targets .sh-stage/.sh-plinth/.sh-spot/.sh-info ? it does NOT target .sh-guitar or .sh-reflect (they are children of .sh-stage), so add explicit `html.rr-reduce-motion #store-hero.show .sh-guitar, ... .sh-reflect { animation: none; transform: translateX(-50%); }` selectors (don't assume the block already covers them, as the doc implies). Keep amplitude tiny (~6-10px / ~1.2deg). No JS/input touch. Bump ?v.


## SYNTHESIZED SHEET (Phase 4 plan + design batch)

All citations verified against the live code. Here is the build plan.

# Reactive Rhythm ? Phase 4 Build Plan

## A) PHASE 4 IMPLEMENTATION PLAN ? Shadow-chart CPU rival + device-scoped GH strict-strum

### KEYBOARD-SAFETY GUARANTEE (read first ? the contract every step below honors)

The keyboard scoring path is **byte-identical** after this phase. These exact paths stay **untouched** ? do not edit a single line in them:

- **keydown** `game.js:2884` ? `game.js:2925` `if (k in keyMap) { e.preventDefault(); if (!e.repeat) { laneDown[keyMap[k]] = true; onLaneInput(keyMap[k], 'key', e.timeStamp); } }`
- **keyup** `game.js:2929` ? `onLaneRelease(keyMap[k])`
- **`onLaneInput`** `game.js:2694` ? **`handleHit`** `game.js:3035` (judge math, scoring, whiff branch `3055`)

These never reference `_frets`, `requireStrum()`, `tryStrum()`, `pollGuitarAxes()`, or `strumCfg`. Verified in code: all strum machinery is reachable **only** from `pollGamepad` (`game.js:2999`, a `navigator.getGamepads()` source disjoint from the keydown listener), and every GH branch is gated by `requireStrum()` (`game.js:2969` = `laneProfile === 'gh' && !!guitarPadId()`), which is **false for any keyboard session** (no guitar pad). The new `strumCfg.strict` flag is added to the `strumCfg` literal (`game.js:39-41`) and read **only** inside `requireStrum()`-gated sites ? it is deliberately **not** added to `applySettings`/`getSettings`, so the keyboard-reachable Settings surface stays blind to it. 4a/4d live entirely in `multiplayer.js`, which does not run during solo keyboard play.

**Per-edit gate:** `node --check game.js && node --check multiplayer.js` (both pass at baseline). Edit via the **Edit tool / python only** ? never a PowerShell rewrite (BOM-less UTF-8 ? cp1252 mojibake). **Bump `?v`** (currently v327/build90) on every game.js / multiplayer.js / index.html edit. Do **not** strip dev hooks (no content-freeze). Keyboard regression check after each batch: confirm `__rrDebug.press(lane)` still scores identically and grep proves no `keyMap`/keydown path references `strict`/`_frets`.

---

### 4a ? Independent shadow-chart CPU rival (multiplayer.js, solo-keyboard-blind)

**The bug (confirmed live):** `multiplayer.js:2869` sets `lastOppState = { sc: Math.round((stt ? stt.score : 0) * skill + el * 600), ... }` where `stt = RhythmGame.getLiveStats()` and `skill ? 0.92`. The CPU score is the **player's own score ? 0.92**, so `os.sc < me.score` always ? `showWinner`'s `me.score > op.score` (`multiplayer.js:1233`) always resolves WIN. **You cannot lose.**

Ordered steps:

1. **Add `_npcSim` state object** near the `_npcRaf` declaration in `devVsNpc` (`multiplayer.js:2843`), reset at run start, with **no read of the player's score**:
   `{ rating, score:0, combo:0, maxCombo:0, hits:0, misses:0, total:0, seen:Set, od:0 }`.
2. **Rate from difficulty:** map the existing `_devBotDiff` / `o.skill` intent to a per-note hit probability ? easy ? 0.78, medium ? 0.90, hard ? 0.965, plus small per-run jitter so two runs differ.
3. **Rewrite the `ghost()` rAF loop** (`multiplayer.js:2864-2873`) to drive `_npcSim` off the **chart**, not the player:
   - Each frame read upcoming notes via `window.RhythmGame.getGhostNotes(aheadSec)` (`game.js:6208`). A note crosses the strike line when its `d` crosses ~0.
   - **Dedupe** each note with a stable id in `_npcSim.seen`. **Risk #1:** `getGhostNotes` returns `{lane,d,type}` over a reused 96-note pool with no stable key ? keying on `d` alone double-judges. *Graft from runner-up `state-stream` (its risk #1):* key the seen-Set on **lane + rounded note-time** and only score on the `d`-crosses-from->0-to-<=0 transition. If `time` isn't derivable from `{lane,d}`, add a **1-line read-only export** on `getGhostNotes` (`game.js:6208`) returning a stable per-note key ? read-only, cannot touch keyboard timing.
   - On each newly-crossed **non-bomb** note: roll `Math.random() < _npcSim.rating` ? HIT: `score += 1500 * npcMult` (`npcMult` from `_npcSim.combo` via the same tier math as `curMult` `game.js:2706`, **hard-clamped to MAX_MULT=4** ? Risk #2, reuse the exact clamp at `game.js:2126` so the ghost stays within the `notes_total*1500` ceiling), `combo++`; else MISS: `combo=0`, `misses++`. **Bombs** (`type===3`): correct play = never hit ? always "avoid," no combo change.
4. **Keep writing `lastOppState`/`lastOppTick`** in the same shape the rival deck consumes (`multiplayer.js:923` reads combo trend) ? but populate `sc/cb/od/ev` from `_npcSim`, not `stt`. Emit the `ev` hit/miss sparkles from the **actual simulated judgments**, so the deck shows true independent play.
5. **Final verdict:** in the `onSongEnd` hook (`multiplayer.js:2855-2858`) build `oppFinal` from `_npcSim`: `{ name, score:_npcSim.score, combo:_npcSim.maxCombo, acc: round(hits/total), grade: gradeFromAcc(...) }` instead of the mirrored `os.sc`. `settleIfReady` ? `showWinner` then yields a **real win OR loss** via the existing `me.score > op.score` compare (`multiplayer.js:1233`) ? **no change to that compare**.
6. **Keep `oppMeta.bot = true`** so `recordMpResult` is still skipped for CPU on the ranked ladder (`multiplayer.js:1238`) ? a real loss to CPU shows on the card but does not tank RP ("CPU warm-ups never count").

**Teardown:** clear `_npcSim` / cancel any pending state on `matchLive` false and at rematch reset.

**Fast-follow (out of 4a critical path):** the tournament drives `devDriveRival` (`multiplayer.js:2211`) and `devDriveBots` (`multiplayer.js:2767`) still ramp toward a random target. Migrate them to the same shadow-sim in a **second pass** ? call out as a known inconsistency, not a blocker.

---

### 4b/4c ? GH strict-strum (game.js, all behind `requireStrum() && strumCfg.strict`)  *hardware-tune-later (no guitar yet)*

Every step here is dead code in any keyboard session (`requireStrum()` false). Defaults are **OFF** = today's behavior byte-identical. Magnitudes (35ms, penalty size) are owner-locked **hardware-tune-later** ? ship default-OFF, dial on a real guitar.

7. **Config (4c base):** add `strict:false` to the `strumCfg` default literal (`game.js:39-41`); it rides the existing `loadStrumCfg`/`saveStrumCfg` (`game.js:44-45`) so it persists in `rr_strumcfg`. Add `const FRET_SETTLE_MS = 35;` near `STRUM_DEBOUNCE_MS` (`game.js:38`).
8. **4b-i Overstrum penalty** ? in `tryStrum` (`game.js:2970`). Today `game.js:2973` `if (_frets.size === 0) return;` silently no-ops. Replace with a strict-gated branch:
   `if (_frets.size === 0) { if (strumCfg.strict && state === 'playing') applyOverstrum(); return; }`
   `applyOverstrum()`: a small GH-authentic streak-killer ? `combo = 0; comboTierCur = 0` (reuse the exact combo-break lines from the bomb/drop paths `game.js:3066` / `2742`) + a light `registerMissFx` + `playMissSfx` (`game.js:3071`) so it reads. **No score bleed** beyond a tiny amount. This is deliberately stricter than the keyboard whiff (`game.js:3055`, which never breaks combo) and can fire **only** on the gamepad strum bar.
   - **All-whiff overstrum:** have `tryStrum` tally how many of its `onLaneInput` calls (`game.js:2974`) produced a real judgment vs a whiff; if **zero judged + strict**, call `applyOverstrum()` once.
9. **4b-ii Chord fret-settle grace (~35ms)** ? in `tryStrum` (`game.js:2974`) and the fret-down branch (`game.js:3011`). When strict and a strum fires, **defer** the per-lane `onLaneInput` by `FRET_SETTLE_MS`, collecting any frets pressed within the window into the same chord, then judge them together **at the original strum timestamp** (`now`, not fire time ? Risk #3: judge at captured `now` or timing skews). *Graft from runner-up `state-stream` (its risk #4):* **snapshot `_frets` at strum time** and resolve the snapshot, so a fret **released** during the settle window can't drop a chord member. Default OFF = today's instant judge at `game.js:2974` verbatim.
   - **Teardown:** the `_frets.clear()` at run boundary (`game.js:2193`) must also clear any pending settle/chord-defer state and cancel its timer (Risk #3).
10. **4c Strict toggle UI** ? add a toggle in the GH controller-setup block near the `gh-strum-status` row (`game.js:5950-5951`, the "REQUIRE STRUM: ON/OFF" line), wired through `window.RhythmGame.setStrumCfg({strict:bool})` (setter exists `game.js:900`). *Graft from runner-up `state-stream`:* place it **inside the GH-detected (`gh-badge`) block** so keyboard-only players never even see the control, and mirror the existing `set-fail`/`set-chart` seg pattern for consistency; optionally extend `__rrDebug.strumState` to report `strict`. Copy is marked **"hardware-tune-later."**

---

### 4d ? Winner-card RP delta + rank-up (multiplayer.js + index.html, ranked-human only)

The data already exists: `recordMpResult` (`multiplayer.js:225`) mutates and **returns `r`** (with `r.points`, confirmed at `multiplayer.js:237`); deltas are +25 win / +8 draw / ?12 loss (`227-229`); `mpRankTier(points)` (`216`) gives the tier; the rank-up sting already fires at `231`. Today `showWinner` records but throws the delta away.

11. **Markup:** in the `.mpx-step-winner` block (`index.html:4349-4369`), under `#mpx-verdict`, add `<div class="mpx-rp-delta" id="mpx-rp-delta" hidden></div>`.
12. **CSS:** near `.mpx-verdict` (`index.html:3361`), add `.mpx-rp-delta` ? **gold `#e0a93f`** for a gain, **warm-grey `#8a807c`** for a loss (brand law R?G?B, no purple/blue; gold is the reserved owned/rank color). *Graft from runner-up `minimal-intercept`:* use the existing `set()` helper to write it and refresh `paintRankChip` (`multiplayer.js:242`/lobby chip) so the card and lobby agree.
13. **Wiring:** in `showWinner`, just before the existing record block (`multiplayer.js:1238`), capture `var _before = getRank().points` and `var _beforeTier = getRank().tier` (read **before** the call ? `recordMpResult` pre-captures only the tier name, not points). Capture the returned `r` at the `recordMpResult` call (`multiplayer.js:1241`); compute `_delta = r.points - _before` and `_rankUp = mpRankTier(r.points).n !== _beforeTier`. Paint `#mpx-rp-delta`: `"+25 RP"` / `"-12 RP"`, plus a `"RANK UP ? SILVER!"` line when `_rankUp` (visual counterpart to the existing sting).
14. **Gating:** render **only** inside the same `!_rankRecorded && !spectating && !(oppMeta && oppMeta.bot)` guard (`multiplayer.js:1238`). A CPU match (now losable via 4a) leaves `#mpx-rp-delta` hidden ? CPU never touches the ladder. `resetForRematch` (`multiplayer.js:1250`) and `step('winner')` must clear/hide it so a stale delta can't linger.

---

### Phase 4 file-touch summary

- **game.js** (4b/4c, all behind `requireStrum()`/`strumCfg.strict`): `strict:false` at `39-41`; `FRET_SETTLE_MS` near `38`; `tryStrum` `2970-2975` (overstrum branch + all-whiff tally + chord-settle defer with `_frets` snapshot); new `applyOverstrum()` helper; fret-down `3011` feeds the settle collector; `2193` clears pending settle state; strict toggle UI in the `gh-badge`/`gh-strum-status` block `~5950`; optional 1-line read-only `getGhostNotes` key export `6208`.
- **multiplayer.js** (4a + 4d): `_npcSim` + rewritten `ghost()` loop in `devVsNpc` `2843`/`2864-2873`; `oppFinal` from `_npcSim` at `onSongEnd` `2855`; `showWinner` `1238` RP-delta capture + paint; `resetForRematch` `1250` hide/clear. Fast-follow: `devDriveRival` `2211` + `devDriveBots` `2767`.
- **index.html** (4d): `#mpx-rp-delta` in `.mpx-step-winner` `4349-4369`; `.mpx-rp-delta` CSS near `3361`; bump `?v`.

---

## B) DESIGN-EVOLUTION DO-NOW BATCH (keyboard-safe + brand-safe)

All 14 items below are render-layer / overlay / CSS only ? none touch the keyboard input or scoring path. Brand-safe (warm darks R?G?B, gold reserved for owned/armed/rank, crimson reserved for action/danger, no purple/blue). Per item: `node --check game.js` after any JS edit, bump `?v`, edit via Edit tool / python.

### Area 1 ? First-90s teaching / on-ramp (the biggest gap)

- **[M] Key glyph baked onto catcher rings** ? `game.js` render catcher loop after `drawCatcher` (`game.js:4348`). Add eased `_catcherKeyHint` (1.0 at song start ? 0 over ~7s songTime; force 0 once `rr_career`/`rr_scores` exist so veterans never see it; reset in `resetScoring` `game.js:2180`). `ctx.fillText` the `keyGlyph(keyForLane(i))` in warm chrome `rgba(220,215,210,a)`, centered on `nearX[i],cY`. Read `keyForLane(i)` so remapped/GH players see their real key; skip on `has-touch`/GH-strum; gate `reduceMotion`/`fxLite`.
- **[M] First-note "PRESS" beacon** ? `game.js` `buildNotes` (tag `notes[0]._firstHint = true` **after** the final sort, near `~L1620`, gated on `!localStorage.rr_career && !localStorage.rr_scores`; do **not** otherwise alter the chart ? notes stay time-sorted, hit-detection early-breaks on that). Draw in the note loop (`~L4503`): when `n._firstHint && !n.judged`, an extra sin-pulsed ring + small "PRESS" caption in warm chrome; clear the flag the instant it's judged. Pairs with the glyph (which key + when). Strictly one-note/one-time.
- **[S] First-run Easy nudge ? persistent inline chip** ? replace the lone 2.6s toast (`game.js:2627`) with a durable gold/chrome pill by the difficulty selector, shown only while `_firstRunEasy && !localStorage.rr_diff` ("You're on EASY ? bump it up anytime"). Reuse the `.si-flag.featured` gold treatment (`index.html:1186`). Hide it in the existing diff-btn click handler (`game.js:2612-2617`) so it auto-clears the moment difficulty is touched; never re-show once `rr_diff` is set. Markup/overlay only.
- **[S] Whiff cue teaches once** ? in the empty-press branch (`game.js:3057`): if first-run (`!rr_career`/`!rr_scores`) && `!localStorage.rr_whiff_taught` ? `flashJudgment('NO NOTE ? WAIT FOR ONE', '#b9b2ac', true)` and set the flag; else keep today's `flashJudgment('?', '#8a807c', true)`. **Keep `isWhiff=true`** (won't clobber a real callout, won't stamp `_lastRealJudgeMs`); leave `playWhiffSfx`/`catcherRecoil`/no-combo guardrail byte-identical. Warm chrome, **not** crimson (crimson would read as "you failed").

### Area 2 ? Gameplay juice (combo / Overdrive feedback)

- **[S] Overdrive final-2s burn-down cue** ? inside the `if (odActive)` block (`game.js:4661-4670`): when `odTimer < 1.6`, scale the pulse frequency by `(1 + (1.6 - odTimer))` (faster breathe) and lerp the edge stop (`game.js:4666`) from gold `(255,180,90)` toward crimson `(255,80,70)` by `t=(1.6-odTimer)/1.6` ? a wordless countdown, still `screen`/additive so it never reads purple. Add one soft descending tick at the ~1.5s mark in the drain block (`~game.js:4108`), guarded by `!muted`. Add `JUICE.odWarnAt` (`game.js:127` + presets `129-131`) for `__rrJuice` tuning. Cosmetic only ? never touches `odTimer`/scoring; floor the ramp under `reduceMotion`.
- **[M] Approach-to-milestone tension band** ? declare `let comboApproachT = 0;` near `comboMidT` (`game.js:222`), decay in loop (`~4052`). In `handleHit` after the milestone/mid blocks (`~3115`): `const toGo = 25 - (combo % 25);` and when `toGo <= 4 && combo >= 21` set `comboApproachT = (5 - toGo) / 5`. In render near the `comboMidT` band (`~4595`) draw a thin warm gold/crimson edge-glow scaled by `comboApproachT` so the screen tightens before the lightning lands; optionally nudge `comboGlow` up per approach note. Cap below the band so it never hurts note-read; skip `fxLite`/`reduceMotion`. Must **not** change mult/score.
- **[S] Perfect-only catcher snap ring** ? in `spawnHitParticles` `if (isPerfect && !reduced)` (`game.js:3710-3713`), push ONE ultra-short **stroked** hairline ring `{ring:true, stroke:true, x:laneX, y:hitY, age:0, life:0.07, color:'255,255,255', max:lw*0.55}` (add a thin `stroke` branch in the ring draw if absent). Pair with a 1-frame catcher-rim white flash via `laneHitPulse[lane]`. At the catcher only ? **no** center-screen flare (energy only where play happens). White core = on-brand chrome. No timing/scoring change.

### Area 3 ? Store showroom feel

- **[S] Hero idle float + spotlight drift (CSS-only)** ? `index.html` `.sh-guitar` (`~1106`), `.sh-reflect` (`1108`), `.sh-spot` (`1102/1157`). Add `@keyframes shIdle{0%,100%{transform:translateY(0)}50%{transform:translateY(-8px)}}` as a **second** animation after the one-shot `shRise` (`animation: shRise .5s var(--ease-settle) both, shIdle 5s ease-in-out .5s infinite`). Mirror the inverse float onto `.sh-reflect` (it's `scaleY(-1)`); slow translateX drift on `.sh-spot`. For `#store-hero.is-cover` (`1116`) float but **do not rotate** (a flat card spinning reads cheap). **Critical:** the reduce-motion kill block (`1166-1168`) does **not** cover `.sh-guitar`/`.sh-reflect` (they're children) ? add explicit `html.rr-reduce-motion #store-hero.show .sh-guitar, ? .sh-reflect { animation:none; transform:translateX(-50%); }`. Amplitude tiny (~6-10px / ~1.2?). (This consolidates the two duplicate hero-float entries in the triage into one.)
- **[M] Bonus-Sparks balance + "almost there" nudge** ? `index.html` `renderHero()` not-owned branch (`8076-8081`). After `bp=bonusPriceFor(it)`, read `have=(RC()&&RC().getBonusSparks)?RC().getBonusSparks():0` (mirror the guard at `8167`). If `have>=bp` render "You have {fmt(have)} Bonus" in **gold** (`.sh-price.bonus` already uses `--gold`); else "You have {fmt(have)} ? {fmt(bp-have)} more by playing" in `--ink-dim`, **never crimson** (a paywall-red slap is off-brand). New `.sh-bonus-have` class (Chakra Petch ~11px). Render regardless of `signedIn` (bonus needs no auth). Overlay only.
- **[M] "Unlocked!" / "Equipped" celebration beat** ? `index.html` add an absolutely-positioned `.sh-claimed` inside `.sh-stage` (z above `.sh-guitar`). Write `celebrateHero(kind)`: if a hero is open (`_heroItem`), add a class triggering a one-shot ~700ms gold radial ring-expand + plinth flare (reuse `--gold` + `.sh-plinth` glow) + a "NOW YOURS"/"EQUIPPED" wordmark fade, auto-remove on `animationend`. Call `celebrateHero('bought')` in `buy()`'s `res.ok` block (after `8154`) and `celebrateHero('equipped')` in `equipSkin` after `7912`. **Gold only** ? no crimson confetti (crimson = action). Under `rr-reduce-motion` swap for an instant static stamp. Non-blocking ? never trap focus. Fires only from store clicks.

### Area 4 ? Token / theme consolidation (CSS hygiene, brand consistency)

- **[S] Shared easing tokens in `:root`** ? `index.html` `:root` (`32-48`): add `--ease-settle: cubic-bezier(.2,1,.3,1); --ease-spring: cubic-bezier(.2,1.4,.3,1); --ease-bar: cubic-bezier(.2,.8,.2,1);`. Swap the literal settle curve at its **real** 15+ sites (105, 605, 747, 842, 812-813, 1019, 1155-1158, 3128, 3291, 2255, 2304), the `(.2,.8,.2,1)` bar curve (710, 1214, 733, 3153, 3207, 3229) ? `var(--ease-bar)`, and `badgePop`'s spring (2269, 2273-2274, 2283) ? `var(--ease-spring)`. Re-point the `.count` 3-2-1 keyframe (`1460`) to `var(--ease-settle)`. JS untouched. Grep `cubic-bezier` after to catch strays; reduce-motion damp (`51-56`) still honored.
- **[S] Re-skin results timing + chase blocks to tokens** ? replace `#b9b2ac` (`game.js:2524`), `#8a847d` (`game.js:2526`), `#b9b2ac` (`catalog.js:1277`) with `var(--ink-dim)`; replace the histogram non-on-beat bar `rgba(236,231,227,0.45)` (`game.js:2522`) with a `--chrome`-based class. **Keep the on-beat bar literal `#e0a93f`** (`game.js:2522`, == `--gold`; canvas/inline-string code can't read CSS vars). Best form: lift both inline blobs into real `.rr-timing`/`.rr-chase` classes. `node --check game.js` after (template literals).
- **[S] Consolidate gold-pulse keyframes ? but keep odpulse separate** ? define ONE canonical pure-gold `@keyframes goldReady` (from `storeArmPulse`'s gold values) and re-point `storeArmPulse` (`1088`) + `vsodpulse` (`1533`) + the `gauge.od.ready` arm states (`1805/1845`) to it (per-site duration only). **Leave `odpulse` (`1810`) SEPARATE** ? it starts crimson `rgba(255,60,40,0.4)` and warms to gold (an intentional OD identity); unifying it would tint the buy-confirm/versus-OD pulses crimson (reads as "danger/cancel" on a buy). Note `odpulse` has 6 consumers incl. non-OD ones (`2273, 2274, 4661, 4720`) ? wider blast radius than assumed; grep for removed keyframe names before deleting.

### LATER (not this batch)

- **Local versus** engine-doubling + 2-person split-screen test (Task #156, DEFERRED ? needs a second device).
- **AI Flixs playable level** + the Local-Versus XL build (Task #165, Phase 6 ? XL scope).
- **Tournament bot drives** (`devDriveRival` / `devDriveBots`) shadow-sim migration ? fast-follow after 4a lands.
- **Server-authoritative MP scoring** ? existing TODO; a losable CPU doesn't change the client-trusted ladder reality.
- **Strip dev hooks** (`__rrDebug`, `__rrChartStats`, `__mpDev`, FPS meter, `?dev`/`?novideo`/`?fps`/`?mock`) ? only at owner-declared content-freeze, not now.