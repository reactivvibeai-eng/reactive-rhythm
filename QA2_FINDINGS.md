# QA round #2 + design-evolution — findings (build88 / Phases 1-3)

Source: workflow wte4yd46k (20 agents, adversarial+verified). 10 confirmed bugs, 24 design ideas.


## A. CONFIRMED BUGS


### store-hero-escape-1  [minor]  (store-hero close/Escape)
**Escape over the showroom hero closes the ENTIRE store, not just the hero**
- file: index.html — store IIFE: hero Escape handler (line ~8103) vs store-screen Escape handler (lines 8256-8258)
- evidence: The store-screen Escape handler is registered on `window` capture-phase: `window.addEventListener('keydown', function(e){ if(e.key==='Escape' && screen.classList.contains('active')){ e.stopImmediatePropagation(); close(); } }, true);` (8256-8258). The hero's own Escape handler is registered on `document` capture-phase (8103): `document.addEventListener('keydown', ... , true)`. In DOM capture phase, listeners fire from the outermost ancestor inward, so `window`'s capture listener runs BEFORE `document`'s. `openStoreHero` (8041) layers the hero (z-index 1200) but never removes `.active` from the store `screen`, so `screen.classList.contains('active')` is still true. The window handler therefore fires first, calls `stopImmediatePropagation()` (killing the hero's document handler), and runs `close()` — which tears down the whole Sparks shop.
- why: A user inspecting a guitar in the showroom and pressing Escape (the universal 'back out of this detail view' gesture) is dumped all the way out of the store instead of back to the grid. The hero's dedicated Escape-to-close is dead code in practice. The code comment on 8103 ('capture-phase so it wins over the global Escape router') is wrong — the store's own window-level handler outranks it.
- fix: Make the hero's Escape win when it is open. Either (a) move the hero's keydown listener to `window` capture AND register it so it runs first, then short-circuit the store-screen handler when the hero is showing — e.g. in the store-screen handler add `if ($('store-hero').classList.contains('show')) return;` so Escape only closes the screen when no hero is up; or (b) in `openStoreHero` strip `.active` interaction by gating the screen handler on hero state. Simplest: guard line 8257 with `&& !$('store-hero').classList.contains('show')` and let the hero handler (also needs to fire) close the hero.
- keyboardRisk: False
- verdict: CONFIRMED in the live shipped code at D:\sunoai music plan\animev1\veo 3 round 2\can i pet that dog\cloudcode\v2\index.html (store IIFE starts at line 7761 with `var screen = $('store-screen')`).

Evidence read directly:
- Line 8103 (hero Escape handler): `document.addEventListener('keydown', function (e) { if (e.key === 'Escape' && hero.classList.contains('show')) { e.stopPropagation(); e.stopImmediatePropagation(); closeStoreHero(); } }, true);` — registered on DOCUMENT, capture phase.
- Lines 8256-8258 (store-screen Escape handler): `window.addEventListener('keydown', function (e) { if (e.k

### store-hero-focustrap-1  [minor]  (store-hero a11y / focus trap)
**showroom hero is aria-modal but does not trap focus — Tab escapes to the grid behind it**
- file: index.html — #store-hero markup (line 4905, role="dialog" aria-modal="true") + wireHeroOnce (8099-8104) + openStoreHero (8041-8062)
- evidence: #store-hero is declared `role="dialog" aria-modal="true"` (4905). On open, `openStoreHero` only focuses the close button (`cb.focus()`, 8061) and `closeStoreHero` restores `_heroPrevFocus`. Nothing sets the store grid behind it to `inert` or `aria-hidden`, and there is no Tab-cycling keydown handler. A grep for `inert`/`tabindex` in the store IIFE shows none applied to `#store-grid` when the hero opens; the grid cards (`cards[i].style.cursor='pointer'`, clickable) and each grid `.store-buy` button remain in the tab order.
- why: A keyboard or screen-reader user inside the 'modal' showroom can Tab off the close/Buy buttons straight into the hidden grid behind it — focus lands on controls they can't see, and they can even trigger a grid Buy from behind the modal. Violates the aria-modal contract.
- fix: On open, set `$('store-grid')` (or the whole `.store-card`) `inert`/`aria-hidden="true"` and restore on close, OR add a focus-trap keydown handler on the hero that cycles Tab/Shift+Tab between `.sh-close` and the action buttons. Clear the attribute in `closeStoreHero`.
- keyboardRisk: False
- verdict: CONFIRMED at code level in D:\...\v2\index.html. (1) #store-hero is declared role="dialog" aria-modal="true" (line 4905) and is position:fixed; inset:0; z-index:1200 with a semi-transparent backdrop (#store-hero rule, line 1092) — so the store grid behind it stays rendered, in the DOM, and in the tab order. (2) openStoreHero (8041-8062) only focuses the close button (cb.focus(), line 8061); it sets hero aria-hidden=false and class 'show' but does NOTHING to the background — no inert, no aria-hidden on #store-grid or .store-card. (3) wireHeroOnce (8099-8104) wires only sh-close click, backdrop 

### results-stale-chase-1  [minor]  (Results reward loop — next-chase CTA)
**Stale #results-chase CTA lingers into a failed / zero-score / no-track run (wrong song's Best shown)**
- file: catalog.js recordLocal (chase block ~L1214-1227; removal only at L1217) + game.js renderResults (L2459-2572) / showScreen (L281-292)
- evidence: The #results-chase node is inserted as a sibling of the static persistent <p id="results-blurb"> (index.html:5404) via insertAdjacentHTML('afterend',...) and is ONLY removed at catalog.js:1217 (`const _ex = document.getElementById('results-chase'); if (_ex) _ex.remove();`). That removal sits INSIDE the per-song block guarded by `if (!failed && currentTrack && currentTrack.id && results.score > 0)` (catalog.js:1193). renderResults cleans #results-timing each render (game.js:2502) but never touches #results-chase; showScreen('results') (game.js:286) only toggles .active classes. So when the NEXT run is failed (runFailed→results.failed true), or has results.score===0 (an all-miss run: score stays 0, endGame L2383 Math.round(0)=0), or has no currentTrack (engine-only demo, currentTrack stays null per catalog.js:862), the block is skipped and the prior run's chase line ('Best 60,000 — beat it by 10,000', possibly for a DIFFERENT song) stays painted under the new (e.g. 'Signal lost') blurb.
- why: On the reward screen a player who fails or zero-scores a run sees a leftover 'Best N — beat it by M' chase line that references the previous (often different) song — misleading reward feedback that contradicts the current result.
- fix: Remove #results-chase unconditionally each results render — e.g. in renderResults alongside the #results-timing cleanup (game.js:2502): `const _oc = document.getElementById('results-chase'); if (_oc) _oc.remove();` — so it only persists when recordLocal re-adds it for a qualifying run.
- keyboardRisk: False
- verdict: CONFIRMED at the code level. Every load-bearing assertion holds:

1. #results-chase is inserted via insertAdjacentHTML('afterend', ...) as a SIBLING of persistent results markup — anchored to #results-timing (or #results-blurb fallback) at catalog.js:1221-1225. The blurb <p id="results-blurb"> at index.html:5404 is static screen markup, not regenerated.

2. The ONLY removal of #results-chase is catalog.js:1217 (`const _ex = document.getElementById('results-chase'); if (_ex) _ex.remove();`), and it sits INSIDE the per-song block guarded at catalog.js:1193 by `if (!failed && currentTrack && curr

### results-newbest-firstplay-2  [nit]  (Results reward loop — NEW BEST +X delta)
**First-ever play prints 'NEW BEST +<entire score>' as if beating a prior best by the whole score**
- file: catalog.js recordLocal L1199-1210
- evidence: isNewBest = !prevReal || results.score > (prevReal.score||0) (L1199); when prevReal is null (first play of the song) isNewBest is true and `_delta = results.score - ((prevReal && prevReal.score) || 0)` (L1209) equals the full score, so the badge reads '★ NEW BEST +' + _delta.toLocaleString() (L1210) e.g. '★ NEW BEST +50,000' on a run that had no prior best to beat.
- why: The '+X' on NEW BEST reads as 'you beat your previous best by X'; on a first-ever play X is your entire score, which is semantically confusing (there was no previous best). Cosmetic only.
- fix: Show the '+X' delta only when prevReal exists (a genuine improvement); for the first-ever best render a plain '★ NEW BEST' with no +delta. e.g. `prevReal ? '★ NEW BEST +' + _delta.toLocaleString() : '★ NEW BEST'`.
- keyboardRisk: False
- verdict: Confirmed by reading catalog.js L1193-1213 (recordLocal). On a first-ever play of a song, the difficulty-scan loop at L1197 leaves prevReal=null, so L1199 isNewBest = !prevReal || ... is true, and L1209 _delta = results.score - ((prevReal && prevReal.score) || 0) evaluates to the full score (minus 0). L1210 then renders '★ NEW BEST +' + _delta.toLocaleString(), so a first run scoring 50,000 literally prints '★ NEW BEST +50,000'. The described user-visible behavior is real and reproducible.

However severity is nit, not minor: (1) it is documented-intentional — the L1208 comment explicitly stat

### od-pill-touch-1  [minor]  (OD banner)
**On-highway OVERDRIVE READY pill hardcodes "PRESS SPACE" — wrong/misleading on touch devices**
- file: game.js: render() OD-ready pill, ~line 4707 (sub-line) within the `if (state==='playing' && overdrive>=1 && !odActive)` block at ~line 4680
- evidence: The P3B on-highway pill draws `ctx.fillText('PRESS  SPACE', cx, by + h*0.34)` with NO touch branch — its only gate is `state==='playing' && overdrive>=1 && !odActive`. Every OTHER Overdrive cue in the codebase pairs SPACE with the tap affordance: index.html:4762 How-To says "Hit SPACE (or tap the flame)", the gauge label index.html:5291 is "OVERDRIVE · SPACE" next to the tappable #od-flame button (whose aria-label/title at index.html:5287 is "tap the flame"). `document.body` already carries `has-touch` (game.js:422) and the countdown coachmark keys off it (game.js:2306). On mobile, Overdrive is activated by the #od-flame tap handler (game.js:2862) — there is no Space key — yet this large, pulsing, playfield-dominant pill tells the player to press one.
- why: On phones/tablets the single most prominent Overdrive-ready cue instructs the player to press a key that doesn't exist, so touch players may never discover that Overdrive fires by tapping the flame — the meter fills, the banner shouts PRESS SPACE, and nothing they can do matches it. Directly undercuts the OD reward loop on the platform where the tap-zone coachmark and `has-touch` styling otherwise carefully adapt.
- fix: Branch the sub-line on touch: e.g. `const _spaceCue = document.body.classList.contains('has-touch') ? 'TAP THE FLAME' : 'PRESS  SPACE';` and render that. (The HUD od-text at game.js:3885 and the flashJudgment('OVERDRIVE READY') at 3887 have the same touch-agnostic wording and could get the same treatment, but the on-highway pill is the load-bearing one.)
- keyboardRisk: False
- verdict: Confirmed by reading the live code at D:\sunoai music plan\animev1\veo 3 round 2\can i pet that dog\cloudcode\v2\game.js. The on-highway OVERDRIVE READY pill at game.js:4707 renders ctx.fillText('PRESS  SPACE', cx, by + h*0.34) with NO touch branch; its only gate (game.js:4680) is state==='playing' && overdrive>=1 && !odActive — a large pulsing playfield-dominant element (width/height set at 4685). The codebase already distinguishes touch: document.body gets 'has-touch' at game.js:422 (navigator.maxTouchPoints>0 || 'ontouchstart' in window), and other UI keys off it (countdown coachmark, game.

### whiff-color-warm-1  [nit]  (whiff cue)
**Whiff-cue judgment color #8a7f86 is a cool/purple-leaning grey — breaks the warm-dark (R>=G>=B) brand rule**
- file: game.js: handleHit() empty-press branch, line 3046 — `flashJudgment('—', '#8a7f86')`
- evidence: #8a7f86 decodes to R=138, G=127, B=134. R>=G holds but B(134) > G(127), so it is NOT R>=G>=B — it is a blue-tinted (cool/purple) grey. By contrast the existing 'LATE' chrome tick color #dad7d2 (R=218,G=215,B=210) is properly warm (R>=G>=B). Brand law for this project is warm darks only (R>=G>=B), NO purple/blue.
- why: The whiff dash flashes on the central #judge-flash popup every time the player taps an empty lane (common in normal play), so a faintly-purple grey appears repeatedly in the player's focal point — exactly the cast the owner has repeatedly rejected.
- fix: Use a warm neutral grey, e.g. #877f7a or #8a807c (R>=G>=B), to match the existing chrome (#dad7d2 family) instead of #8a7f86.
- keyboardRisk: False
- verdict: Confirmed by reading the cited code. game.js line 3046, in handleHit()'s empty-press (whiff) branch: `flashJudgment('—', '#8a7f86')`. Channel decode is accurate: #8a7f86 = R138,G127,B134, so B(134) > G(127) — it violates the project's brand law of warm darks only (R>=G>=B, no purple/blue). The established chrome judgment color #dad7d2 (used at line 85 for PERFECT and line 3811 for LATE) correctly decodes to R218,G215,B210 (R>=G>=B, warm), confirming the whiff grey is the odd one out with a faint cool/magenta cast.

User-visibility verified: flashJudgment writes to #judge-flash, which index.htm

### countdown-race-1  [minor]  (countdown)
**A rapid re-launch starts a second runCountdown while the first is still mid-flight, both writing the shared #countdown node**
- file: game.js: runCountdown() lines 2296-2333; beginPlay() await at line 2266; restartGame()/play() entry
- evidence: runCountdown() is a self-contained async fn that does NOT check _playGen. beginPlay() awaits it at line 2266; the gen-guard only runs AFTER the await resolves (line 2267), so a superseding launch (++_playGen in the new beginPlay) cannot interrupt the in-flight countdown — the old runCountdown runs its full ~2.4s of setTimeouts to completion, including its final `screens.countdown.classList.remove('active')` (line 2329) and repeated `el.textContent = i` writes to the same $('countdown'). If a second launch (e.g. a fast double-tap on a Levels grid card / jukebox — launchTrack at catalog.js:1094/play() set no re-entry lock; only #play-btn is disabled during beginPlay) starts its own runCountdown in that window, the two race on the same DOM node: digits flicker and the OLD countdown's remove('active') can hide the NEW countdown overlay mid-count.
- why: On a quick re-launch the 3-2-1 can visibly flicker (old digits overwrite the new GET READY/3) or the overlay can vanish a beat early, making the lead-in feel broken. Scoring is safe (the gen-guard ensures only the newest run actually plays), so impact is cosmetic and edge-gated.
- fix: Tag the countdown with the launch gen and bail/early-return its loop + skip the trailing remove('active') when superseded, e.g. capture `const myGen = _playGen` at entry and `if (myGen !== _playGen) return;` after each await before mutating `el`/`screens.countdown`. Or add a re-entry lock to play()/launchTrack so a second launch can't begin while one is in its pre-roll.
- keyboardRisk: False
- verdict: CONFIRMED at the code level. Read game.js:2296-2333 (runCountdown), 2211-2294 (beginPlay), 2335-2348 (stopGame/restartGame), 1969-1986 (play), 6151-6152 (playUrl/playDemo), catalog.js:1117-1146 (launchTrack), and index.html:5378-5379 + game.js:279 (the singleton DOM nodes).

Verified facts:
1. runCountdown() (game.js:2296) is a self-contained async fn with NO _playGen check inside. Its delays are anonymous `await new Promise(r=>setTimeout(r,...))` with no stored handle, so stopGame() (which only cancels rafId/player, lines 2335-2346) cannot cancel them — a superseded countdown runs its full ~3

### whiff-clobber-judgment-1  [minor]  (whiff cue)
**Whiff '—' can overwrite a simultaneous real judgment on the shared #judge-flash popup**
- file: game.js: flashJudgment() line 3796-3800 (single shared el) + whiff call line 3046 + real-hit calls (e.g. line 3093/flashJudgment in handleHit)
- evidence: flashJudgment() writes ONE shared element `$('judge-flash')` (line 3797-3798). In 5-lane keyboard play a player frequently presses two lanes near-simultaneously; if lane A lands a note (real judgment flashJudgment, e.g. a STREAK/PERFECT cue) and lane B is empty, lane B's empty-press branch fires `flashJudgment('—', '#8a7f86')` (line 3046). Both are independent keydown events with no ordering guarantee, so the grey '—' can be the LAST writer and clobber the satisfying real callout.
- why: A correct hit's feedback can be visually replaced by a dull grey dash in the same frame, undercutting the dopamine of the hit and making good play feel mushy — the opposite of the intended game-feel guardrail.
- fix: Suppress the whiff flash when a real judgment fired this frame (e.g. a short `_lastJudgeT` timestamp set on real hits that the whiff branch checks), or give the whiff its own subtler element / lane-local cue instead of the central #judge-flash popup. Do NOT change any scoring path.
- keyboardRisk: False
- verdict: CONFIRMED mechanism, but trivial consequence. flashJudgment() (game.js:3796-3800) writes ONE shared #judge-flash element (textContent + color + animation-restart), with no queue/coalesce. The whiff branch calls flashJudgment('—', '#8a7f86') on an empty press (line 3046); real hits call it for GREAT/GOOD (line 3151), STREAK milestones (3093), HOLD (2711/2728), BOMB (3064), etc. Keyboard input is dispatched as separate synchronous keydown events (line 2874-2916 → onLaneInput line 2684 → handleHit line 3025), one fully completing before the next, so within a single paint frame two flashJudgment c

### coach-reduce-motion-1  [minor]  (Mobile onboarding / reduced-motion)
**Tap-zone coachmark + outline animations ignore the in-app reduce-motion toggle (only honor OS-level)**
- file: index.html:1896-1909 (rrTapCoach / rrTapCoachLbl keyframes + the @media guard); game.js:2130 applyReduceMotion()
- evidence: The coachmark animations are guarded ONLY by `@media (prefers-reduced-motion: reduce) { #tap-zones.coach .tap-zone::after, #tap-zones.coach::before { animation: none; } }` at index.html:1907-1909. The app also exposes an IN-APP reduce-motion control: game.js:2130 `applyReduceMotion()` toggles `document.documentElement.classList('rr-reduce-motion', reduceMotion)`, and the JS reduce-checks (game.js:6253-6256, 8755-8758) treat `rr-reduce-motion` class, the OS media query, AND `fxLite` as equivalent reduce signals. Every other animated element pairs BOTH guards — e.g. index.html:1185-1186 (atom), 3185-3186 (#menu-hub tiles), 3188-3190 (start-fore) all have `html.rr-reduce-motion ... { animation: none; }` next to the `@media` variant. The `#tap-zones.coach` pulse (and the persistent `.tap-zone::after` outline) have only the `@media` form, so they have NO `html.rr-reduce-motion` rule.
- why: A touch player who enabled reduce-motion via the in-app Settings toggle (OS setting NOT set) still gets the pulsing 'TAP THE LANES' coachmark + pulsing lane outlines on their very first countdown. This is exactly the photosensitive/calm-visuals audience the toggle exists for, and it is a first-run moment they cannot replay or pre-disable. The build86 CHANGELOG verify note (line 4174) implies reduced-motion is honored, but it is only honored for the OS-level query, not the app's own contract.
- fix: Add an in-app-class sibling to the existing guard: `html.rr-reduce-motion #tap-zones.coach .tap-zone::after, html.rr-reduce-motion #tap-zones.coach::before { animation: none; }` alongside the @media block at index.html:1907-1909 (mirroring the dual-guard pattern used everywhere else). CSS-only; cannot affect keyboard play.
- keyboardRisk: False
- verdict: Confirmed by reading the cited code. index.html:1891-1909: the #tap-zones.coach lane-outline pulse (rrTapCoach, line 1894) and the "TAP THE LANES" caption (rrTapCoachLbl, line 1904) are guarded against animation ONLY by `@media (prefers-reduced-motion: reduce) { #tap-zones.coach .tap-zone::after, #tap-zones.coach::before { animation: none; } }` (lines 1907-1909). Grep confirms these are the ONLY #tap-zones.coach rules — there is no `html.rr-reduce-motion` sibling. The in-app reduce-motion control is real and independent of the OS query: game.js:146 declares `reduceMotion`, game.js:180-181 load

### firstrun-toast-info-nit-1  [nit]  (First-run nudge / Easy default)
**_firstRunEasy toast passes an unsupported 'info' severity that is silently coerced to neutral**
- file: game.js:2617 (showToast call) vs game.js:2000-2001 (showToast severity allowlist)
- evidence: game.js:2617 calls `window.RhythmGame.showToast('Starting you on EASY — bump it up anytime in difficulty ▸', 'info')`. showToast (game.js:2000-2001) only accepts `severity === 'error' || 'success' || 'neutral'`; anything else (including 'info') falls through to `sev = 'neutral'`. So the 'info' argument is dead — the toast renders as neutral with the neutral glyph regardless.
- why: No functional break — the first-run Easy nudge still appears with neutral styling. It is a latent dead argument: if someone later adds real 'info' styling/glyph to _TOAST_GLYPHS expecting this call to use it, this nudge silently won't.
- fix: Change the argument to a supported value (e.g. `'neutral'`) at game.js:2617, or add 'info' to the showToast allowlist + _TOAST_GLYPHS map if a distinct info style is intended.
- keyboardRisk: False
- verdict: Independently confirmed all cited code in the live root D:\...\cloudcode\v2\game.js and index.html.

EVIDENCE (confirmed exactly):
- game.js:2617 — `window.RhythmGame.showToast('Starting you on EASY — bump it up anytime in difficulty ▸', 'info')` passes severity 'info'. This is the ONLY call site passing 'info' (grep confirmed unique).
- game.js:2001 — `const sev = (severity === 'error' || severity === 'success' || severity === 'neutral') ? severity : 'neutral';` — 'info' is not in the allowlist, so it is coerced to 'neutral'.
- game.js:1994-1998 — `_TOAST_GLYPHS` has only error/success/neutra


## B. DESIGN-EVOLUTION IDEAS


### Overdrive "final 2 seconds" burn-down cue (visual + audio anticipation before it snaps off)  [S/high]  (Overdrive / game-feel)
- rationale: Right now Overdrive ends abruptly: loop() line 4098 just sets odActive=false and fires one emitFx('odend') shockwave when odTimer<=0 — there is zero anticipation. The euphoric part of Star Power in GH is the *clock pressure* of milking the last seconds. Giving the player a felt "it's running out, cash in NOW" beat the morning-after makes the whole OD window read as a deliberate arc (ignition you already nailed -> sustain -> urgent burn-down -> release) instead of a flat block that vanishes.
- sketch: game.js render(), inside the existing `if (odActive)` gold-vignette block (~line 4648): when `odTimer < 1.6`, ramp the vignette pulse faster (scale the sin() frequency by (1 + (1.6-odTimer))) and lerp the edge color from gold (255,180,90) toward crimson (255,80,70) so the frame visibly "cools" as it empties — a wordless countdown. Optionally add a tiny `OD_DURATION` burn-down ring identical to the odBurst ring but inward. For audio, in loop()'s drain block add a one-shot at the 1.5s mark (a soft descending tick reusing the existing AC synth path from hitFeel's fallback branch, guarded by `!muted`). Tune via a new JUICE field e.g. JUICE.odWarnAt.
- risk: Must stay additive gold and never strobe (reduce-motion already floors odVigPulse); keep it cosmetic only so it cannot touch odTimer/scoring. No keyboard path touched — Space still the only activator.

### Approach-to-milestone tension band (combo 20-24, 45-49, …) — the 5-note runway into the lightning  [M/high]  (Combo / dopamine)
- rationale: The combo reward map has a gap I confirmed in handleHit: a full LIGHTNING at combo%25===0 (line 3084) and a single MID pulse at combo%25===15 (line 3102), but the *approach* (the 4-5 hits right before a milestone) has no rising tension — the payoff arrives with no wind-up. Thumper/GH magic is the crescendo INTO the hit, not just the hit. A subtle escalating shimmer on the last few notes before each century/milestone turns every 25th hit into an event you can *feel coming*, which is exactly the kind of polish you only notice after sleeping on it.
- sketch: game.js handleHit, after the milestone/mid blocks: compute `const toGo = 25 - (combo % 25);` and when `toGo <= 4 && combo >= 21`, scale a new `comboApproachT` (decays in loop like comboMidT) by (5-toGo)/5. In render, draw a thin warm edge-glow that brightens each successive note — reuse the comboMidT screen-band gradient (line 4582) at lower alpha, intensifying as toGo->0 so the screen literally tightens right before the lightning lands. Optional: nudge `comboGlow` up a touch per approach note so the strings pre-heat into the milestone.
- risk: Cosmetic only — must not change mult/score. Keep it gold/crimson and cap intensity so it never competes with note-read clarity (the existing burst code already worries about occluding the read-path). Skip under fxLite/reduceMotion like the sibling FX.

### Perfect-only catcher signature (a crisp white-core ring snap that GREAT/GOOD don't get)  [S/med]  (Hit feedback / juice)
- rationale: v253 (line 3147-3151) correctly suppresses the PERFECT *word* so a clean run stays uncluttered — good call. But that means the catcher button itself gives no perfect-exclusive tell: spawnHitParticles scales the same burst by intensity, so PERFECT vs GREAT differ only in particle count, not in a distinct *snap* you register peripherally. The best rhythm games make a perfect feel categorically different (OSU's 300, GH's spark color). A sub-frame white-core ring that fires ONLY on perfect — at the catcher, not center-screen — gives the owner's keyboard runs a constant subliminal "yes, dead-on" confirmation that's the soul of tight play.
- sketch: game.js spawnHitParticles (~line 3696): the L2 perfect double-ring already exists but reads as volume, not signature. Add a single ultra-short (life 0.07) thin bright-white STROKED ring (not filled) at exactly lw*0.55 that only spawns on isPerfect — a hairline 'click' snap distinct from the soft filled shockwaves. Pair with a 1px catcher-rim white flash by bumping laneHitPulse's white component for one frame. Gate behind `!reduced`. This makes PERFECT read instantly without a word.
- risk: Keep it tiny and at the catcher (doctrine: energy only where play happens — the code comments repeatedly enforce this). White core is on-brand (chrome/white-hot). Must not add a center-screen flare (explicitly removed in build19/build20). No timing/scoring change.

### First-fill Overdrive READY: make the very first time the meter fills a real moment  [S/med]  (Overdrive / onboarding-juice)
- rationale: updateHUD line 3887 fires flashJudgment('OVERDRIVE READY') once per fill via odReadyAnnounced, and the on-highway pill (line 4680) pulses every fill identically. But the *first* fill of a session is the teaching moment — that's when a new player learns Space pays off. Treating fill #1 the same as fill #5 wastes the single best chance to make the mechanic land. A slightly bigger, slower first-fill reveal (and a one-time keycap glint on the word SPACE) converts the feature from 'noticed' to 'understood', which is the morning-after upgrade for the Easy-default first-run player you just shipped.
- sketch: game.js: add a session flag `_odFirstReady` (or localStorage rr_odseen for once-ever). In the pill render block (line 4680), when it's the first-ever ready, run a 1.2s entrance scale/alpha on the pill (reuse the offsetWidth-restart pattern) and add a brief animated underline/glint sweeping across the 'PRESS SPACE' sub-line (a moving linear-gradient highlight in canvas). After it's seen once, fall back to today's steady pulse. No effect on the HUD chip path.
- risk: One-time per session/localStorage only, so veterans aren't slowed. Stays brand gold; pill already exists so this is an emphasis layer, not new chrome. Pure presentation — Space behavior unchanged.

### Timing-tick "in the pocket" reward — reward consistency, not just flag error  [M/med]  (Results + live feedback / mastery)
- rationale: flashTiming (line 3805) shows EARLY/LATE ms, and the results histogram (line 2500) shows bias — both are corrective (they tell you what's WRONG). There's no positive reinforcement for a *streak of dead-on hits*, which is the addictive core of mastery-driven rhythm games. A subtle 'IN THE POCKET' state when several consecutive hits land inside the perfect window gives the skilled keyboard player (the owner) a reason to chase precision beyond the score number — and it's leaderboard-safe because it's purely a feel/label layer over data you already collect in _timingSamples.
- sketch: game.js handleHit: track a rolling counter of consecutive `kind==='perfect'` (reset on great/good/miss). At a threshold (e.g. 8) set a `_pocketT` glow and swap the timing-tick (or a small new pip) to '◎ IN THE POCKET' in gold for a beat; keep escalating wording at 16/32. On results, add a 'best pocket streak: N' line beside the existing timing histogram block (insertAdjacentHTML, same pattern as line 2518). All derived from existing per-hit data.
- risk: Must not alter scoring or the perf-window math. Keep the cue gold and small near the timing-tick element so it doesn't fight the combo readout. Off under reduce-motion. Histogram-on-results already proven, so the data plumbing exists.

### Strike-line impact "give" — the catcher row dips on the beat / on heavy hits (weight)  [M/med]  (Game-feel / weight)
- rationale: Hits currently push the individual button down (lanePulse, line 3026) and the whole scene shakes (cameraShake), but the strike LINE itself — the row the player reads against — is rigid. The juiciest rhythm games give the impact a sense of *mass*: a chord or a perfect should make the catcher rail itself flex a hair, so playing feels like striking something physical rather than triggering particles over a static bar. Confirmed there's a per-frame `--rr-beat` CSS var already exported (line 4024) and an audio-reactive bgPulse, so the hook to drive a subtle rail-dip already exists — it's just not used for vertical give on the strike row.
- sketch: game.js drawCatcher / catcher-row render (~line 4308-4427): add a transient `_railDip` (px) driven by max(bgPulse-on-beat, recent perfect/chord laneHitPulse), eased toward 0 in loop. Apply it ONLY as a `ctx.translate(0, railDip)` around the catcher visuals (rim/glow/buttons) — explicitly NOT around noteY/hit math. Cap at ~lw*0.04. Tune via a new JUICE.railDip field so it ships behind the live __rrJuice config you already have.
- risk: Geometry is measured and locked to the guitar art (CLAUDE.md hard rule) — the dip must be a tiny COSMETIC render offset (a few px, eased) applied only to the catcher-row draw, never to fretGeom()/note hit positions, or it would desync hit detection. Keep amplitude small; respect reduceMotion. Keyboard timing is computed from songTime, not pixels, so it's safe.

### Key glyph baked onto the catcher rings (first 8s, fades out)  [M/high]  (in-game HUD / highway (game.js drawCatcher + a fadeable opacity hook))
- rationale: The single biggest first-90s gap I found: during play a keyboard newcomer sees five IDENTICAL glowing rings at the bottom of the neck (game.js ~4327-4349 drawCatcher draws only rings, no glyph) and the ONLY key reference is the .footer-hint strip — 11px, color:var(--ink-dim), bottom:24px (index.html L5370, CSS L2315), nowhere near where their eyes are (the falling notes). Printing the lane's actual key (A S D J K) faintly ON each catcher for the opening seconds puts the map exactly where the action is, then dissolves so it never clutters a veteran's board. The 'oh, THAT's which key' moment you only notice you were missing after sleeping on it.
- sketch: In game.js render loop, after the drawCatcher(nearX[i]...) call (~L4335), add an opacity term _catcherKeyHint (1 at song start, eased to 0 over ~7s of songTime, 0 entirely once runsPlayed>~3). When >0.02: ctx.save(); font Oxanium ~lw*0.16, fillStyle 'rgba(236,231,227,'+a+')', textAlign center, fillText(keyForLane(i).toUpperCase(), nearX[i], cY+lw*0.02). Only on keyboard profile (skip when has-touch / GH-strum). Reset the timer in resetScoring().
- risk: Must read keyForLane(lane) (already the source of truth used by updateFooterHint, game.js L2150-2153) so a remapped/GH-profile player sees their real key, not a hardcoded A-S-D-J-K. Pure render-layer (ctx.fillText after drawCatcher); touches NO input path, so keyboard play stays byte-identical. Brand: chrome/warm-grey glyph at low alpha, never blue. Gate behind a first-N-runs check (read rr_career/rr_scores like _firstRunEasy does) + reduceMotion so it doesn't nag returning players or fight fxLite.

### First-note 'PRESS' beacon — guarantee the first win  [M/high]  (in-game highway + buildNotes head tag (game.js))
- rationale: Right now GET READY -> 3-2-1 -> GO (game.js runCountdown L2296) is generic, then notes just start falling. A brand-new player's very first note is indistinguishable from the rest, so their first input is a guess — and a missed first note is the worst possible first impression. A one-time pulsing 'PRESS' ring that rides the FIRST note down its lane (only on a player's first-ever song) converts the first note into a guaranteed teaching win — the dopamine spike that hooks the session. Pairs with the catcher glyph: glyph says WHICH key, beacon says WHEN.
- sketch: In buildNotes, after the final sort, if it's a true first run set notes[0]._firstHint=true (gate on no rr_career && no rr_scores). In the note render branch (game.js ~L4352), when n._firstHint && !n.judged, draw an extra eased ring at nx,ny scaled with a sin pulse + a small 'PRESS' label in chrome. On judge, clear the flag. No SFX, no input change.
- risk: Do NOT alter note timing or the chart (buildNotes must stay byte-identical for keyboard scoring integrity — notes are time-sorted and hit-detection early-breaks on that). Render-only: tag notes[0] with a _firstHint flag and draw a pulsing ring + 'PRESS' caption as it approaches, cleared the instant it's judged. First-ever-song gate via rr_career/rr_scores absence (the signal _firstRunEasy already reads, game.js L2613). Risk: feels patronizing if it ever shows twice — must be strictly one-note, one-time.

### Whiff cue speaks the lesson the first time it happens  [S/med]  (in-game judgment (game.js empty-press branch L3045-3049))
- rationale: The shipped whiff cue is great game-feel — empty press => flashJudgment('—') + dry tick + half catcher recoil, and crucially NO combo/stability hit (game.js L3045-3049). But to a confused beginner '—' is cryptic: they don't learn WHY nothing happened. The very first time a new player whiffs, swapping '—' for one human caption ('NO NOTE THERE — WAIT FOR ONE') turns a silent dead-end into a micro-lesson, then reverts to the clean '—' forever after. The penalty-free guardrail is already perfect; this just makes the first instance legible.
- sketch: In game.js L3046, gate: if first run && !rr_whiff_taught -> flashJudgment('NO NOTE — WAIT FOR ONE', '#b9b2ac') and set localStorage rr_whiff_taught='1'; else flashJudgment('—','#8a7f86'). playWhiffSfx() + catcherRecoil[lane] unchanged. Nothing else in the branch moves.
- risk: Must NOT change the no-combo/no-stability behavior (that guardrail is the whole point and is keyboard-safe). Only the displayed string + duration change, and only once (a localStorage rr_whiff_taught flag so it's truly once-ever). Keep the caption short or it overruns the judge-flash slot. Brand-neutral chrome color, NOT crimson — crimson reads as 'you failed', the wrong message for a penalty-free event.

### Make the first-run Easy nudge unmissable (inline chip, not a 2.6s toast)  [S/med]  (first-run difficulty (game.js _firstRunEasy L2613-2617) + a difficulty-control affordance)
- rationale: The Easy on-ramp is real and well-built (_firstRunEasy auto-sets easy when no rr_diff AND no history, game.js L2613-2615), but the only signal is a single showToast on a 2600ms setTimeout (L2617). Toasts auto-dismiss and a new player drinking in the title art will miss it entirely — so they never learn they're on Easy OR that they can climb. A persistent 'You're on EASY — bump it up anytime' chip on the difficulty control (until they touch difficulty once) makes the on-ramp self-explaining and kills the 'why is this so slow?' confusion for anyone arriving from a harder rhythm game.
- sketch: Add a small inline badge by the difficulty selector, shown only while _firstRunEasy && !localStorage rr_diff. In the existing diff-btn click handler (game.js L2603) that already sets rr_diff, also hide the badge. Keep the toast as a secondary cue but make the inline chip the durable one. CSS: gold-border pill reusing the store NEW badge style already in index.html.
- risk: Must auto-clear the moment the player changes difficulty — the diff-btn click handler at game.js L2603-2607 already writes rr_diff, so clear the chip there — so it never nags a returning player. Keep it a quiet gold/chrome label, never a modal (a modal would block the dopamine of just pressing play). Don't re-show once rr_diff is ever set.

### Overdrive's first availability gets an in-play 'try it' teach, once  [S/med]  (in-game OD readiness (game.js updateHUD OD block L3880-3888))
- rationale: The OD system teaches SPACE well — but ONLY once the meter is FULL: flashJudgment('OVERDRIVE READY') (L3887) + the on-highway pill (L4677). A true beginner on Easy may never build enough combo in their first song to fill it, so Overdrive — a core dopamine system — stays invisible in-play and lives only in a How-To overlay they may have dismissed. A one-time breadcrumb the FIRST time a new player's meter crosses ~50% ('Keep your combo — then SPACE') lands the existence of the system even if they never reach 100% that run.
- sketch: In updateHUD's OD block (game.js ~L3883-3885), when first-run && overdrive crosses ~0.5 && !rr_od_taught, set #hud-od-text to a one-time 'KEEP YOUR COMBO — THEN SPACE' for ~2s, then set rr_od_taught. Falls back to the normal 'CHARGING · N%' string. Pure text; no new DOM, no input change.
- risk: Don't touch activateOverdrive() or the SPACE binding (keyboard-safe). The odReadyAnnounced one-shot pattern (game.js L121, L3887) is the exact idiom to copy. Risk of clutter for veterans — gate strictly on first-run history absence + a localStorage rr_od_taught so it shows at most once ever. Put it on the existing #hud-od-text (L3885 already swaps CHARGING/READY/ACTIVE strings) rather than new DOM, so it can't overlap the playfield.

### Results 'what to do next' for the very first scorecard  [S/med]  (results screen (game.js renderResults L2498-2516 + catalog.js chase CTA L1264-1275))
- rationale: The results screen is rich for a returning player — early/late timing histogram (game.js L2500-2516) and a NEW BEST delta + 'beat it by N' chase CTA (catalog.js L1264-1275). But it's ALL comparative: the chase CTA only renders when prevReal.score>0 (catalog.js L1268) and the histogram needs >=4 samples (L2503). On a player's FIRST EVER scorecard there's no prior best, so the single most motivating element — the next-step nudge — is absent exactly when a new player most needs a reason to hit play again. A first-run-only 'Play one more — your timing sharpens fast' CTA fills that void and starts the retention loop on run #1.
- sketch: In catalog.js renderResults augmentation (~L1264), add an else branch: if isFirstRun (no prior best && low career runs) and !isNewBest, inject a #results-firststep div after the same _anchor with a warm one-liner framing 'play again'. Reuse the chase CTA's inline style block. Remove any prior instance first, exactly like #results-chase.
- risk: Insert at the same anchor the chase CTA uses (results-timing || results-blurb, catalog.js L1270) and REMOVE it on subsequent renders (the code already removes #results-chase each render at L1267 — mirror that) so it appears only when there's no prior best. Don't let it collide with the FULL COMBO / GRADE UP badges. Post-run screen, zero keyboard/input risk. Brand: chrome/gold Chakra Petch matching the existing chase CTA inline style at L1272.

### Hero showroom: slow idle float + spotlight drift so the guitar feels alive, not photographed  [S/high]  (Store / showroom hero (index.html #store-hero .sh-stage))
- rationale: The hero is the 'fall in love before you buy' moment, but right now .sh-guitar (index.html:1106) is dead-still after its one-shot shRise entrance, the .sh-reflect is a flat scaleY(-1) flip, and the .sh-spot just blooms once (shBloom, 1157) then freezes. A real showroom turntable breathes — a 4-6s vertical float + a barely-perceptible spotlight sway reads as 'this is a coveted object on a stand,' the kind of micro-life you only register the morning after as 'that felt premium.' It costs nothing in JS and is the single biggest feel upgrade per byte.
- sketch: index.html CSS only. Add @keyframes shIdle{0%,100%{transform:translateY(0)}50%{transform:translateY(-8px)}} on .sh-guitar, applied as a SECOND animation that starts after shRise finishes (animation: shRise .5s ... , shIdle 5s ease-in-out .5s infinite). Add a slow translateX drift to .sh-spot. Mirror the float onto .sh-reflect inverted so the reflection tracks. Add both to the existing rr-reduce-motion kill block at 1166. Bump ?v. No game.js touch, no input touch.
- risk: None to keyboard (pure CSS on an overlay that never has focus during play). Must be gated behind html.rr-reduce-motion (the file already has that block at 1166-1168 — extend it). Keep amplitude tiny (~6-10px / ~1.2deg) or it reads as wobble, not luxury; cover-art levels (#store-hero.is-cover) should float but not rotate (a flat card spinning looks cheap).

### Show the player's Bonus-Sparks balance + an 'almost there' nudge right inside the hero pricebox  [M/high]  (Store / dual-currency (index.html renderHero + .sh-pricebox))
- rationale: renderHero (index.html:8063) prints 'price OR <bonus price>' but never tells the player whether they can actually afford the Bonus path or how close they are. A player admiring a guitar with 1,800 Bonus Sparks staring at a 2,100 Bonus price has no idea they're one good run away — that's the exact moment that should pull them back into a song, the healthy non-predatory version of a spend funnel. Surfacing 'You have 1,800 — earn 300 more by playing' turns earned currency from an abstract number into a tangible, reachable goal, which is what makes spending it feel earned and rewarding rather than arbitrary.
- sketch: In renderHero's not-owned branch (index.html:8071-8078): after computing bp=bonusPriceFor(it), read have=RC().getBonusSparks(). If bp>0 append a small line under .sh-pricebox: if have>=bp show 'You have {have} Bonus' in gold; else show 'You have {have} — {bp-have} more by playing' in ink-dim with a tiny SPARK glyph. New .sh-bonus-have CSS class (Chakra Petch 11px, matches .sh-reassure scale at 1140). Only render when signed-in path isn't required (Bonus needs no sign-in per buyBonus at 8184).
- risk: Keyboard-safe (overlay-only). Brand: balance text stays gold only when it's the owned/earned Bonus line (gold is the sanctioned earned-currency color here — .sh-price.bonus already uses --gold at 1131); the 'short by N' nudge must stay neutral chrome/ink-dim, never red-alarm (don't make a F2P 'you can't afford it' feel like a paywall slap). Read getBonusSparks via the RC() guard already used at setBonusBalance (8161) so it no-ops if catalog is absent.

### A genuine 'unlocked!' beat on the hero stage after a purchase/equip, instead of a silent re-render  [M/high]  (Store / buy + equip feedback (index.html buy()/equipSkin/renderHero))
- rationale: Right now the payoff for buying or equipping a guitar is a toast and a quiet card flip — buy() (8146) and equipSkin (7907) both just toast() + render(). The most emotional 5% of the whole store funnel (the instant the guitar becomes YOURS) has zero on-stage celebration. A brief gold ring-burst + 'NOW YOURS' / 'EQUIPPED' stamp sweeping across the hero plinth is the dopamine cash-out that makes a purchase feel like a reward, not a transaction — and it reinforces gold-as-ownership, which is exactly the brand's reserved meaning for gold.
- sketch: index.html. Add a transient .sh-claimed element over .sh-stage (absolute, z above .sh-guitar). In buy() success (8141) and equipSkin (7907), if _heroItem is open, call a new celebrateHero(kind) that adds a class triggering a one-shot @keyframes (gold radial ring expand + plinth::after flare + a 'NOW YOURS'/'EQUIPPED' wordmark fade). Reuse the existing --gold and .sh-plinth glow vars. Auto-remove the class on animationend. Guard with rr-reduce-motion → just flash the stamp opacity 0→1→hold.
- risk: Keyboard byte-identical (fires only from store click handlers, never touches the input router). Must respect rr-reduce-motion (swap the burst for an instant static stamp). Keep it SHORT (~700ms) and non-blocking — don't trap focus or delay the Equip button becoming usable. Gold-only for the flash (sanctioned for owned/equipped); no crimson confetti (crimson is action/brand, gold is the 'it's yours' signal).

### Per-guitar character: real lore + a signature trait chip so each skin has a personality, not a type label  [M/med]  (Store / merchandising copy (index.html sh-traits + blurb + a small SKIN_LORE map))
- rationale: The hero's identity row is generic: sh-traits (index.html:8056) is always [RARITY, TYPE, FREE/SPARKS·BONUS] and .sh-lore falls back to blurb() (7919) → 'Guitar skin — equip to play'. Every guitar in the showroom currently sells itself with the same three words. Players fall in love with a guitar's STORY (the Deadkin carnival axe, the Triemrys, the alarm-clock hero) — a one-line evocative lore string + one signature trait chip ('CARNIVAL-FORGED', 'STORM-TUNED') gives each piece a hook that justifies wanting it. This is the merchandising that turns a sprite into a want.
- sketch: index.html. Add a small SKIN_LORE map keyed by item_id ({deadkin:{lore:'...',trait:'CARNIVAL-FORGED'}, ...}) near STORE_FEATURED (7896). In blurb(), if no meta.description and SKIN_LORE[id], return its lore. In openStoreHero traits build (8056), unshift SKIN_LORE[id].trait as a leading .sh-trait when present. Falls back gracefully to today's behavior for un-authored skins. No new CSS needed (.sh-trait/.sh-lore exist).
- risk: Pure data/copy, zero gameplay/keyboard impact. Risk is tonal, not technical: keep lore short (≤ ~90 chars, fits the 46ch .sh-lore at 1125) and on-brand (dark/crimson mythos, not jokey). Don't invent traits that imply a gameplay buff the skin doesn't have (cosmetic-only — avoid 'EXTRA COMBO' type chips that mislead). Use the existing meta.description first (blurb already prefers it at 7920) so backend copy still wins.

### Soft confirm on the Bonus 'Use Bonus' button to match the real-Sparks arm-state — but framed as a moment, not a guard  [S/med]  (Store / Bonus spend flow (index.html buyBonus + .store-buy.bonus))
- rationale: buy() (real Sparks) has a careful 2-click arm-confirm (8124, 'Confirm · N⚡', gold pulse, 4s disarm) because it's real money — but buyBonus (8184) commits instantly on the first click. That asymmetry means the BIGGER-feeling spend (a 2,100-Bonus guitar you grinded for) gets LESS ceremony than the cash one. A single confirming tap on Bonus isn't about preventing fraud (it can't cost money) — it's about making the player consciously SAVOR cashing in their earned currency. A 'Confirm · {bp} Bonus' beat makes the spend deliberate and rewarding instead of an accidental click.
- sketch: index.html. Add _armedBonus/_disarmBonus mirroring 8118-8119 but using the .store-buy.bonus gold treatment. In buyBonus (8184) gate the first click to arm ('Confirm · {price} Bonus', add .armed) and only commit on the second within 4s. Wire _disarmBonus into close() (8250) and into render()'s reset (8008) alongside _disarmBuy. Keyboard play untouched.
- risk: Keyboard-safe (store buttons only). Don't reuse _armedBuy/_disarmBuy verbatim — those are scoped to the real-Sparks btn and _disarmBuy restores _origLabel; a naive reuse could cross-wire the two arm-states (and render() already calls _disarmBuy at 8008, which must NOT also nuke a Bonus arm mid-rebuild). Give Bonus its own _armedBonus/_armBonusTimer. Keep the pulse gold (matches .store-buy.bonus gold theme at 1065) — do NOT use the crimson arm style. Verify the 4s auto-disarm clears on store close() (8250) like the Sparks one.

### Make the cards' NEW/FEATURED merchandising self-maintaining and time-decaying instead of a hardcoded literal  [S/med]  (Store / card flags (index.html STORE_NEW/STORE_FEATURED + flagOf))
- rationale: flagOf (index.html:7898) reads two hand-edited literals (STORE_NEW at 7897, STORE_FEATURED at 7896). The 'NEW' badge is the cheapest merchandising hook in any F2P store — it's what makes a returning player's eye snap to fresh content — but a hardcoded list goes stale the moment someone forgets to edit it, and worse, 'NEW' stays NEW forever (it's lying after a month). A drop-date-driven NEW flag (auto-expires after N days) keeps the storefront feeling alive and trustworthy with zero ongoing maintenance, and lets FEATURED be a curated spotlight that actually rotates.
- sketch: index.html. Give STORE_NEW entries an optional until-date, or read it.meta.created_at if present; in flagOf compute new = date within last ~21 days. Keep STORE_FEATURED as the curated literal (it's an editorial choice, fine to hand-set). No CSS change (.si-flag.new/.featured exist at 1185-1186). If no date is resolvable, return today's behavior verbatim so it's a safe superset.
- risk: Keyboard-irrelevant. Low technical risk but needs a date source: if items lack a reliable added/created date, fall back to the existing literal so nothing regresses (don't drop a badge that's there today). Keep the 'NEW & owned → no flag' suppression already in itemHtml (flag && !owned at 7993) — an owned item should never scream NEW. Don't let FEATURED and NEW both render on one card (current code already prefers featured; preserve that precedence).

### Adopt one shared --ease-out token across all entrances  [S/high]  (motion / cross-surface easing)
- rationale: After Phases 1-3 the app has at least four different 'arrival' curves doing the same job. The store-hero, modal cards and leaderboard all use cubic-bezier(.2,1,.3,1) (index.html lines 105/842/1019/1155-1158), progress bars use (.2,.8,.2,1) (lines 710/1214), badges use a springy (.2,1.4,.3,1) (lines 2266/2270-2271), and the countdown still uses plain ease-out (line 1460). Nothing names these, so the 'house curve' is an accident of copy-paste and the next surface will diverge again. The thing you notice the morning after is that the store feels buttery and the countdown feels generic — same product, two motion personalities. Promoting the (.2,1,.3,1) settle to a named token and a separate spring token makes every future entrance inherit the same signature for free.
- sketch: In index.html :root (lines 32-48) add --ease-settle: cubic-bezier(.2,1,.3,1); --ease-spring: cubic-bezier(.2,1.4,.3,1); --ease-bar: cubic-bezier(.2,.8,.2,1). Then replace the literal cubic-beziers in cardIn (105), shRise/shFade/shPlinth (1155-1158), lbd-card (842), store-card (1019), bars (710/1214), badgePop (2266/2270-2271) with the vars. Re-point the countdown .count keyframe (1460) from 'ease-out' to var(--ease-settle) so the 3-2-1 shares the house arrival. No JS change.
- risk: Zero keyboard impact (CSS variables only, no input path). Reduced-motion is already globally damped at lines 51-56 so the tokens stay honored. Only risk is missing one literal during the swap, leaving a stray curve — purely cosmetic, caught by a grep for 'cubic-bezier' after.

### Re-skin the results timing + chase blocks to brand tokens  [S/med]  (results screen consistency)
- rationale: The P1 timing histogram (game.js 2513-2516) and the P3 chase CTA (catalog.js 1272-1275) are injected as inline-styled HTML with hardcoded greys — #b9b2ac, #8a847d, rgba(236,231,227,0.45). The brand dim ink is --ink-dim:#a0938f (index.html line 38, deliberately tuned to ~4.7:1 WCAG and kept WARM). #b9b2ac/#8a847d are lighter and drift cooler/neutral, so these two newest blocks sit a half-shade off every other label on the same card (.rbadge, .results-blurb all use the vars). It is the kind of mismatch you don't see in motion but absolutely see in a still screenshot the next morning: the histogram caption is a different grey than the blurb six pixels above it. Pulling them onto --ink-dim / --chrome also re-inherits the documented contrast tuning instead of silently regressing it.
- sketch: In game.js _html (2513-2516): replace color:#b9b2ac -> color:var(--ink-dim), #8a847d -> var(--ink-dim), rgba(236,231,227,0.45) -> var(--chrome) at ~0.45 via a wrapper class instead of inline rgba. In catalog.js results-chase (1272): color:#b9b2ac -> var(--ink-dim). Better: lift both blobs into two real CSS classes (.rr-timing, .rr-chase) in index.html near .results-badges (2249) so they live with the rest of the results styling and stop carrying inline hex.
- risk: Inline styles are string-built in JS; a typo could break the template literal — node --check catches syntax, and it is render-only (no input). The 'on-beat' center bar gold (#e0a93f) should stay literal since it equals --gold and canvas/string code can't read CSS vars anyway; only swap the grey body text.

### Give results blocks the store-hero stagger cascade  [M/high]  (entrance choreography)
- rationale: The store-hero earned a genuinely premium reveal: scrim, plinth scaleX, then a 7-step :nth-child fade cascade at 60ms intervals (index.html 1158-1165). The results screen — the emotional payoff of a run — reveals everything at once: the card animates via cardIn, but the badges all badgePop together and the brand-new timing histogram + chase CTA are injected flat with NO entrance at all (they just appear after insertAdjacentHTML). So the second-most-important screen in the game has weaker choreography than the store. The 'slept on it' realization is that the win screen should feel like the curtain rising in beats — grade, then judgment bar, then timing read, then the chase line — not a single flat paint.
- sketch: Add a .results-card.active > * { animation: cardIn .4s var(--ease-settle) both; } with :nth-child delays mirroring 1159-1165, scoped to the results content column. Since the timing/chase blocks are injected late, add the same entrance class to them in the JS that builds them (game.js 2518, catalog.js 1271) so they fade in rather than pop. Add the rr-reduce-motion escape hatch. Keep badgePop as the louder spring for the actual badges so they still 'land' last.
- risk: Keyboard-safe (Enter/Esc results flow at line ~2556 is unaffected by visual delay). Real risk: staggered entrance must not delay the celebrateResults burst or feel laggy on a fail screen — gate the cascade to non-failed runs and keep total stagger under ~300ms. Reduced-motion must collapse it (mirror the html.rr-reduce-motion override the store-hero already has at 1166).

### Unify the two 'this is your target' cues into one signature  [M/med]  (HUD + on-highway iconography)
- rationale: Phases 1 and 3 shipped two separate 'aim for this' affordances that don't speak the same visual language. The per-song BEST chip (#hud-best, index.html 2017-2019) is static gold text that flips to flat crimson 'BEAT BEST' with no motion (game.js 3857-3859). The on-highway OVERDRIVE READY pill (game.js 4680-4708) is a gold, sin-pulsing, chevron-prefixed (▸) canvas banner. Both are gold 'a thing is ready/available' prompts inches apart, yet one is dead-flat text and the other is animated and chevroned. The morning-after insight: a player's eye learns 'gold + ▸ + pulse = an active goal' from the OD pill, then the BEST chip — also a goal — uses none of that grammar, so the HUD reads as two designers. Aligning the BEST/BEAT-BEST chip to the same gold-to-crimson, chevron, gentle-pulse vocabulary makes the HUD feel authored by one hand.
- sketch: In index.html, give #hud-best a ▸ pseudo-prefix and reuse the existing odpulse keyframe (1810) as a low-amplitude box-shadow breathe only when .beaten is set (so an un-beaten target is calm gold, a just-beaten one celebrates in crimson with the same pulse the OD pill uses). On the canvas side, optionally reuse the chip's exact chevron glyph in the pill text (already '▸ OVERDRIVE READY' at 4702) — they now match. No engine math change; the beaten-flip at 3858-3859 stays byte-identical, you only add a class the CSS reacts to.
- risk: Must NOT touch input or the score-compare logic at game.js 3857-3859 (only the className/text/visual). A pulse on the chip competes for attention with combo/score — keep it subtle (box-shadow breathe, not transform, to avoid layout shift in the HUD column). Brand-safe: gold for the target, crimson on the 'beaten' state, both already in law.

### Make GET READY / GO! a distinct word treatment, not shrunk digits  [M/med]  (countdown entrance polish)
- rationale: The P1 GET READY/GO! beats (game.js 2315-2327) are implemented by reusing the digit's .count keyframe at a smaller clamp font (line 2316). So the words inherit the exact scale-2-to-0.7 fade-out built for a single glyph — fine for '3', but 'GET READY' and 'GO!' read as 'the number that forgot to be a number'. The countdown is the last thing before the drop and the first taste of game feel; it deserves a word treatment with its own weight (letter-spacing, a crimson underbar or bracket echoing the HUD corner-brackets, GO! punching harder than GET READY). This is a pure-polish thing nobody flags in a bug report but everybody feels: the moment before play should crescendo, and right now it's three identical scale-fades plus two shrunk ones.
- sketch: Add a .count.word modifier class toggled in runCountdown (game.js 2315/2325) for the GET READY and GO! frames. In index.html near .count (1453), give .count.word its own keyframe: letter-spacing ramp + a crimson underline bar (linear-gradient, matching .sh-name::after at 1123 for cross-surface echo) and make GO! use --ease-spring with a slightly larger end-scale so it punches. Leave the numeric .count keyframe and all setTimeout values exactly as-is.
- risk: runCountdown is async and awaited inside beginPlay (game.js 2266) — the per-step timing (650/700/320ms) feeds the lead-in, so DO NOT change the durations or the await structure (could desync the 2s music lead-in noted in CHANGELOG #106). Keyboard play is untouched (countdown is pre-roll, no input). Keep clamp font sizes so words still fit the slot on mobile.

### Standardize the gold-pulse vocabulary into one keyframe  [S/med]  (motion de-duplication)
- rationale: Three near-identical gold 'ready/armed' pulses exist after Phase 1-2: storeArmPulse (index.html 1088-1089, the buy-confirm), odpulse (1810), and vsodpulse (1536) — all box-shadow breathes between a dim gold and a bright gold over ~0.7-1.1s. They were each authored locally for their feature (buy-confirm, overdrive gauge, versus OD) with slightly different timings and colors (#ffd98a vs rgba(255,200,120,.95)), so the same conceptual beat — 'this gold thing is live, act on it' — flickers at three slightly different rates across the app. The subtle win is rhythmic: when the store buy-confirm and the OD gauge pulse at the same cadence and amplitude, the whole product feels like it has one heartbeat. Right now they're three metronomes set a hair apart.
- sketch: Define one @keyframes goldReady with the canonical dim->bright gold box-shadow (pick the odpulse values at 1810 as the reference). Re-point storeArmPulse (1088), the .gauge.od.ready (1805) and vsodpulse (1533) consumers to it, passing duration as the only per-site variable (animation: goldReady .9s ...). Drop the two now-dead keyframes. Verify with grep that no other selector references the removed names.
- risk: Purely cosmetic, no input path. Consolidating timings could make the store buy-confirm pulse marginally faster/slower than the 4s auto-disarm window — keep the period unchanged for storeArmPulse if the owner likes its current urgency; the goal is shared color/amplitude, not forcibly identical speed. Stay within gold law (these are all owned/armed states, correctly gold).


## C. SYNTHESIZED BUILD SHEET

The data is complete and verified. I'll write the build sheet now.

# Reactive Rhythm — Phase 4 Build Sheet

Source: verified QA + design-evolution output for shipped Phases 1–3. All bug verdicts independently code-confirmed; severities below use the **verdict** severity (post-review), not the original reporter severity. Brand law in force: warm darks (R≥G≥B), no blue/purple, gold reserved for owned/equipped/Overdrive. Keyboard play stays **byte-identical** — none of the confirmed bugs touch the input path, and every design item is gated to render/overlay layers only.

---

## A) CONFIRMED BUGS TO FIX

> Ordering note: every confirmed bug downgraded to **minor/nit** on verification — there is **no critical or major** defect, and **zero keyboard-input regressions** in the set. All `[KEYBOARD-RISK]` tags below are explicitly **NONE/SAFE** because each fix is store-overlay, results-render, or cosmetic-copy only. Fix top-to-bottom.

### MINOR

**A1 — Escape over the showroom hero closes the entire store** `[KEYBOARD-RISK: none]`
- **File:** `index.html` — store IIFE, store-screen Escape handler at **8256–8258** (vs hero Escape at 8103).
- **Cause:** `window` capture-phase listener (8257) fires before the hero's `document` capture listener (8103); `openStoreHero` never clears `.active`, so the store handler wins and runs `close()`, tearing down the whole shop. The hero's own Escape-to-close is dead code.
- **Fix:** Guard line 8257 — `if (e.key === 'Escape' && screen.classList.contains('active') && !$('store-hero').classList.contains('show'))`. With the window handler skipped while the hero is up, the hero's document-capture handler (8103) then fires and closes only the hero (returns user to the grid).
- **Recoverable today via close button / backdrop click, but Escape is the universal back gesture — fix it.**

**A2 — Stale `#results-chase` CTA lingers into a failed / zero-score / no-track run** `[KEYBOARD-RISK: none]`
- **File:** `game.js` `renderResults` (~2502, alongside the `#results-timing` cleanup); the only removal today is `catalog.js:1217`, gated inside the qualifying-run block.
- **Cause:** `#results-chase` is only removed when `!failed && currentTrack && results.score > 0`; a following fail/zero/demo run leaves the prior (often different) song's "Best N — beat it by M" painted under the new "Signal lost" blurb.
- **Fix:** Remove unconditionally each render in `renderResults` (mirrors the existing `#results-timing`/`#results-bonus` reset): `const _oc = document.getElementById('results-chase'); if (_oc) _oc.remove();`. `recordLocal` re-adds it only for qualifying runs.

**A3 — On-highway "OVERDRIVE READY" pill hardcodes "PRESS SPACE" on touch** `[KEYBOARD-RISK: none — copy only; keyboard path stays correct]`
- **File:** `game.js` render() OD-ready pill sub-line, **~4707** (inside the `state==='playing' && overdrive>=1 && !odActive` block).
- **Cause:** No touch branch; on phones the most prominent OD cue instructs a key that doesn't exist (OD fires via the `#od-flame` tap, game.js:2862).
- **Fix:** `const _spaceCue = document.body.classList.contains('has-touch') ? 'TAP THE FLAME' : 'PRESS  SPACE';` and render that. Apply the same branch to the HUD od-text (~3885) and the `flashJudgment('OVERDRIVE READY')` wording (~3887) while in the file — cheap consistency.

**A4 — Countdown race: rapid re-launch runs two `runCountdown`s on the shared `#countdown` node** `[KEYBOARD-RISK: none — scoring/audio already gen-guarded]`
- **File:** `game.js` `runCountdown()` 2296–2333; awaited in `beginPlay()` at 2266.
- **Cause:** `runCountdown` has no `_playGen` check, so a superseded countdown runs to completion (including its trailing `screens.countdown.classList.remove('active')`), flickering digits or hiding the new overlay a beat early. Edge-gated to fast back-to-back demo/cached/server-charted relaunches.
- **Fix:** Capture `const myGen = _playGen;` at entry; after each `await`, `if (myGen !== _playGen) return;` before mutating `el`/`screens.countdown`, and skip the trailing `remove('active')` when superseded. (Optional hardening: a re-entry lock on `play()`/`launchTrack`.)

**A5 — Whiff `—` can overwrite a simultaneous real judgment on shared `#judge-flash`** `[KEYBOARD-RISK: none — display string only, no scoring path]`
- **File:** `game.js` `flashJudgment()` 3796–3800; whiff call 3046; real-hit calls (GREAT/GOOD 3151, STREAK 3093, HOLD/BOMB).
- **Cause:** Empty-lane keydown dispatched after a hit keydown in the same frame makes the grey dash the last writer. Note: PERFECT deliberately doesn't flash (3151 guard), so the worst case is a GREAT/GOOD/STREAK callout replaced by `—`.
- **Fix:** Set a `_lastJudgeT` timestamp on real hits; in the whiff branch, suppress the `flashJudgment('—', …)` if a real judgment fired within ~1 frame (~20ms). **Do not touch any scoring/SFX/recoil path** — only the flash is gated.

**A6 — Tap-zone coachmark ignores the in-app reduce-motion toggle** `[KEYBOARD-RISK: none — CSS only]`
- **File:** `index.html` 1907–1909 (the `@media (prefers-reduced-motion)` guard for `#tap-zones.coach`).
- **Cause:** Coachmark pulse + lane-outline animation honor only the OS query, not the app's own `html.rr-reduce-motion` class — unlike every other animated element (which dual-guards). Touch first-run, in-app-on + OS-off, one unrepeatable moment.
- **Fix:** Add the sibling rule next to the `@media` block: `html.rr-reduce-motion #tap-zones.coach .tap-zone::after, html.rr-reduce-motion #tap-zones.coach::before { animation: none; }`.

### NIT

**A7 — Whiff judgment color `#8a7f86` is cool/purple-leaning grey (B>G)** `[KEYBOARD-RISK: none]`
- **File:** `game.js:3046` — `flashJudgment('—', '#8a7f86')` (R138 G127 B134 → violates R≥G≥B).
- **Fix:** Use a warm neutral matching the existing chrome family (`#dad7d2`): `#877f7a` or `#8a807c`. (Note: A5's `_lastJudgeT` guard and this color fix touch the same call — do them together.)

**A8 — First-ever play prints "NEW BEST +<entire score>"** `[KEYBOARD-RISK: none]`
- **File:** `catalog.js` `recordLocal` 1209–1210.
- **Note:** Documented-intentional (comment at 1208 says "full score if first-ever"), and the delta is literally correct (0→score). Cosmetic wording polish only.
- **Fix (optional):** `prevReal ? '★ NEW BEST +' + _delta.toLocaleString() : '★ NEW BEST'`. Low priority — ship only if touching this block.

**A9 — `_firstRunEasy` toast passes unsupported `'info'` severity** `[KEYBOARD-RISK: none]`
- **File:** `game.js:2617` vs allowlist at 2000–2001 (coerced silently to `neutral`).
- **Note:** No user-visible misbehavior (neutral is the correct on-brand look anyway). Latent dead-argument only.
- **Fix:** Change the arg to `'neutral'`. Trivial one-token edit.

---

## B) DESIGN-EVOLUTION SHORTLIST

Dropped as redundant/low-value: **OD first-fill reveal** (subsumed by the OD burn-down + first-availability teach already shortlisted — three OD-onboarding items is one too many), and **soft-confirm on the Bonus button** (adds friction to a no-money action for marginal "savor" value; the arm-state asymmetry is acceptable). Everything else is grouped, sized **S/M/L**, and marked **DO-NOW** vs **LATER**.

### Game-feel / juice — the core dopamine layer (highest ROI; DO-NOW cluster)

| Idea | Size | Verdict | Why |
|---|---|---|---|
| **Overdrive "final 2s" burn-down** — ramp vignette pulse + lerp gold→crimson as `odTimer<1.6`, optional inward burn-ring + one-shot 1.5s descending tick. New `JUICE.odWarnAt`. | **S** | **DO-NOW** | Turns OD from a flat block into ignition→sustain→urgent-burn→release. Best impact/byte in the set. Cosmetic only; gold→crimson both in law; floor under reduce-motion. |
| **Perfect-only catcher signature** — single hairline white-cored *stroked* ring (life 0.07) + 1px catcher-rim white flash, only on `isPerfect`, at the catcher (never center-screen). Gate `!reduced`. | **S** | **DO-NOW** | Gives PERFECT a categorical *snap* the owner's keyboard runs read peripherally — the soul of tight play. White core = on-brand chrome/white-hot. |
| **Approach-to-milestone tension band** — `comboApproachT` brightening the last ≤4 notes into each combo%25 lightning (reuse comboMidT band at lower alpha). | **M** | **DO-NOW** | The crescendo *into* the hit is the GH/Thumper magic the current map lacks. Cap intensity below note-read clarity; skip under fxLite/reduceMotion. |
| **Strike-line "give"** — transient `_railDip` (px) from beat/perfect/chord, eased to 0, applied as `ctx.translate(0, railDip)` around catcher visuals **only** — never noteY/fretGeom. Cap `~lw*0.04`, `JUICE.railDip`. | **M** | **LATER** | Real weight upgrade, but geometry is locked to the guitar art — extra care to keep it off hit-math. Ship after the S-tier juice lands and is dialed in. |
| **"In the pocket" precision reward** — rolling consecutive-`perfect` counter; at 8/16/32 flash `◎ IN THE POCKET` (gold, near timing-tick) + results "best pocket streak: N". Off under reduce-motion. | **M** | **LATER** | Positive reinforcement for consistency (current timing feedback is purely corrective). Leaderboard-safe (label over existing `_timingSamples`). Good, but second-wave. |

### First-90-seconds onboarding — the keyboard newcomer's path (DO-NOW; pairs tightly)

| Idea | Size | Verdict | Why |
|---|---|---|---|
| **Key glyph baked onto catcher rings** (first ~7s / runsPlayed≤3, fades out) — `ctx.fillText(keyForLane(i)…)` at low chrome alpha after `drawCatcher`. **Must** read `keyForLane` (remap/GH-aware), keyboard-profile only. | **M** | **DO-NOW** | The single biggest first-90s gap: five identical rings, key reference stranded in an 11px footer. Puts the map where the eyes are, dissolves for veterans. |
| **First-note "PRESS" beacon** (first-ever song only) — tag `notes[0]._firstHint`, draw pulsing ring + "PRESS" caption, cleared on judge. **Do not alter chart/timing.** | **M** | **DO-NOW** | Glyph says *which* key, beacon says *when* — together they guarantee the first win (the hook). Strictly one-note/one-time or it patronizes. |
| **Whiff cue teaches once** — first whiff ever (`rr_whiff_taught`) shows `NO NOTE — WAIT FOR ONE` (chrome `#b9b2ac`, not crimson), then reverts to `—`. No-combo/no-stability guardrail unchanged. | **S** | **DO-NOW** | Makes the cryptic `—` legible exactly once. Tiny. (Coordinate with bugs A5/A7, same branch.) |
| **First-run Easy nudge → persistent inline chip** (not a 2.6s toast) — gold/chrome pill by the difficulty control while `_firstRunEasy && !rr_diff`; cleared in the existing diff-btn handler. | **S** | **DO-NOW** | The on-ramp is invisible if the lone toast auto-dismisses while the player reads title art. Self-explaining; auto-clears so it never nags returners. |
| **OD first-availability teach** — at meter ~50% on a first run (`rr_od_taught`), swap `#hud-od-text` to "KEEP YOUR COMBO — THEN SPACE" for ~2s. No new DOM, no input change. | **S** | **LATER** | Lands OD's existence even if the meter never fills run-1. Good, but lower than the glyph/beacon pair; ship in the onboarding follow-up. |
| **Results "what to do next" for the first scorecard** — first-run-only "Play one more — your timing sharpens fast" CTA at the chase anchor; removed each render like `#results-chase`. | **S** | **LATER** | Starts the retention loop on run #1 (the comparative chase CTA is absent with no prior best). Pairs with A2's cleanup logic — do it in the same results pass. |

### Store / showroom — merchandising & the "it's yours" moment (mixed)

| Idea | Size | Verdict | Why |
|---|---|---|---|
| **Hero idle float + spotlight drift** — second CSS animation `shIdle` (~6–10px / ~1.2°) after `shRise`; mirror onto `.sh-reflect`; cover-art floats but doesn't rotate. Extend the existing rr-reduce-motion kill block. | **S** | **DO-NOW** | Biggest premium-feel upgrade per byte; pure CSS, overlay-only. The "fall in love before you buy" moment currently sits dead-still. |
| **"Unlocked!" beat on hero after buy/equip** — `celebrateHero(kind)`: one-shot gold ring-burst + "NOW YOURS"/"EQUIPPED" stamp (~700ms), reduce-motion = static stamp. Gold only, no crimson. | **M** | **DO-NOW** | The most emotional 5% of the funnel (it becomes YOURS) currently has zero stage celebration. Reinforces gold-as-ownership. |
| **Bonus balance + "almost there" in pricebox** — `have = RC().getBonusSparks()`; "You have N Bonus" (gold) or "N — M more by playing" (ink-dim, never red). | **M** | **DO-NOW** | Turns earned currency into a reachable goal — the healthy, non-predatory pull back into a song. Keep the "short by N" line neutral, never paywall-red. |
| **Per-guitar lore + signature trait chip** — small `SKIN_LORE` map ({lore, trait}); `blurb()` falls back to it; unshift the trait into `sh-traits`. `meta.description` still wins. ≤~90 chars, dark/crimson mythos, no fake gameplay-buff chips. | **M** | **LATER** | Turns sprites into wants, but it's authoring work (per-skin copy) — schedule after the structural store items; degrade gracefully for un-authored skins. |
| **Self-maintaining, time-decaying NEW badge** — `flagOf` computes `new` from a drop/created date (auto-expire ~21d); fall back to today's literal if no date resolves; keep FEATURED curated; preserve NEW-and-owned suppression. | **S** | **LATER** | Stops "NEW" from lying after a month with zero upkeep — but needs a reliable date source; safe-superset fallback first. |

### Cross-surface motion system — one house personality (DO-NOW foundation; do this FIRST in the design pass)

| Idea | Size | Verdict | Why |
|---|---|---|---|
| **Shared easing tokens** — `--ease-settle: cubic-bezier(.2,1,.3,1)`, `--ease-spring: (.2,1.4,.3,1)`, `--ease-bar: (.2,.8,.2,1)` in `:root`; swap the ~8 copy-pasted literals; re-point the countdown from `ease-out` to `--ease-settle`. | **S** | **DO-NOW** | Cheapest unifier — makes every future entrance inherit one signature. Land this token before the entrance-choreography items so they consume it. Grep `cubic-bezier` after to catch strays. |
| **One `goldReady` pulse keyframe** — fold `storeArmPulse`/`odpulse`/`vsodpulse` into one canonical dim→bright gold box-shadow; per-site duration only. (Keep storeArmPulse's period if the owner likes its urgency — share color/amplitude, not necessarily speed.) | **S** | **DO-NOW** | Gives the product one heartbeat (three gold "act now" pulses currently beat a hair apart). All correctly gold/owned-armed states. |
| **Results blocks → brand tokens** — swap `#b9b2ac`/`#8a847d`/`rgba(236,231,227,.45)` in the timing histogram (game.js 2513-16) + chase CTA (catalog.js 1272) for `--ink-dim`/`--chrome`; keep on-beat gold literal. Best: lift into `.rr-timing`/`.rr-chase` CSS classes. | **S** | **DO-NOW** | The two newest results blocks sit a half-shade cool/off vs every other label on the same still screen; re-inherits the documented 4.7:1 warm-dim tuning. |
| **Results stagger cascade** — `:nth-child` cardIn cascade (<300ms total) for grade→judgment→timing→chase, scoped to non-failed runs; add entrance class to the late-injected timing/chase blocks; rr-reduce-motion collapses it; badgePop stays the louder last "land." | **M** | **LATER** | The win screen deserves the store-hero's curtain-rise; do it after the tokens + results re-skin (it consumes both). Gate off fail screens so it never lags a loss. |
| **Unify BEST / BEAT-BEST chip with OD-pill grammar** — give `#hud-best` a `▸` prefix + low-amplitude `odpulse` box-shadow breathe only when `.beaten`; gold target → crimson beaten. No score-compare change (3857-59 byte-identical). | **M** | **LATER** | Two adjacent gold "goal ready" cues speak different motion languages; align them so the HUD reads as one hand. Box-shadow only (no layout shift). |
| **GET READY / GO! word treatment** — `.count.word` modifier: letter-spacing ramp + crimson underbar (echo `.sh-name::after`), GO! on `--ease-spring` with bigger end-scale. **Keep all setTimeout durations + the await structure** (feeds the 2s music lead-in). | **M** | **LATER** | The pre-roll should crescendo, not show shrunk digits — but it's polish; ship after the higher-impact juice. Don't touch timing or you desync the lead-in. |

---

### Recommended execution order
1. **Bugs A1–A6** (minor), then A7–A9 (nits) — all small, none keyboard-touching; clears the board.
2. **Motion-token foundation** (`--ease-*`, `goldReady`, results re-skin) — small, and later items consume these.
3. **Game-feel S-tier** (OD burn-down, perfect-snap) + **onboarding S/M cluster** (catcher glyph, first-note beacon, whiff-teach, Easy chip) — the dopamine + first-90s wins, highest player impact.
4. **Store DO-NOW** (hero float, unlocked beat, Bonus pricebox).
5. **LATER tier** as a follow-up pass (rail-dip, in-the-pocket, OD/results first-run teach, results cascade, BEST-chip unify, countdown words, lore/NEW-badge).

Bump `?v=NN` on every JS/CSS edit. Strip dev hooks only at content-freeze (Track A), never mid-design-pass.